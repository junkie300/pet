# 앱 기획 — 대화 로그 및 의사결정 기록

> 이 파일은 대화 전체에 걸친 기술 검토·의사결정 누적 기록.
> 아이디어 단위로 섹션을 나눈다. 폐기된 것도 이유와 함께 남겨 재검토 낭비를 막는다.

| # | 아이디어 | 상태 | 결론 |
|---|---|---|---|
| 1 | 항공기 좌석 정보 앱 | ❌ 폐기 (2026-08-29) | 데이터 소스 부재 — D-0 참조 |
| 2 | 반려동물 통합 앱 (Android) | 💬 논의중 | 데이터 전량 확보 가능 — 구조 설계 단계 |

---

## 아이디어 #1 — 항공기 좌석 정보 앱  ❌폐기

> 작성 시작: 2026-08-29 · 상태: **구상 단계 (코드 없음)**
> 대화하면서 계속 누적 갱신하는 문서. 결정된 것 / 아직 안 정한 것 / 확인된 사실을 구분해서 기록.

---

### 1. 만들려는 것 (사용자 최초 구상)

비행기 제조사·모델별로 좌석배치, 편한 좌석, 소음, 사고율, 좌석별 생존율을 조회하는 앱.

### 기능 개요 (원안)
1. 제조사 콤보 → 기체모델 콤보 (각각 단일 선택)
2. 선택 모델의 **기본 좌석배치** 표시
   - 항공사별 재배치 시, 사용자가 항공사 좌석배치도 이미지를 업로드해 기본 설계와 겹쳐보기
3. 좌석배치도 상단에 필터 콤보(복수 선택): `편한 좌석 확인`, `좌석별 소음비교`
4. `사고율` 은 별도 메뉴. 단 상단의 선택된 제조사/모델 컨텍스트는 유지

### 검토 요청 사항
1. 제조사·모델별 좌석배치도를 구할 수 있는가
2. 넓은 좌석 / 소음 정보를 알 수 있는가
3. 그 외 정보를 크롤링 또는 API로 받을 수 있는가

---

### 2. 조사로 확인된 사실 (2026-08-29 기준)

### 2.1 [중대] "제조사 → 모델 → 기본 좌석배치"라는 전제가 성립하지 않음

- Boeing/Airbus는 **좌석이 없는 상태의 기체를 인도**한다. 좌석 배치(LOPA)는 전적으로 항공사가 결정.
- 따라서 "787-9의 기본 좌석배치" 같은 것은 **존재하지 않는다.** 같은 787-9라도 항공사마다 좌석 수가 200~400석대까지 갈린다.
- 제조사가 공개하는 유일한 배치도는 ACAP(Airplane Characteristics for Airport Planning) 문서의
  "typical interior arrangement" — 어디까지나 공항 설계용 **예시**이며 실제 운항 기체와 무관.
- 원안 2번의 "항공사 재배치 시 이미지 업로드 오버레이"는 이 문제를 부분적으로 인지한 설계로 보이나,
  실제로는 재배치가 예외가 아니라 **기본값**이므로 오버레이로 감당할 수 있는 규모가 아님.

→ **앱의 데이터 축을 바꿔야 함. 최우선 의사결정 사항 (D-1 참조).**

### 2.2 [중대] SeatGuru 서비스 종료

- TripAdvisor가 **2025-10-31 SeatGuru 폐쇄.** 24년 만. seatguru.com 은 안내문 후 리다이렉트.
- 폐쇄 전 수년간 갱신이 사실상 중단되어 2020년 이전 배치도가 다수 남아있던 상태였음.
- 즉 "예전에 SeatGuru에서 보던 그 데이터"를 그대로 가져올 경로는 **더 이상 없음.**

### 2.3 데이터 소스별 실현 가능성

| 데이터 | 소스 후보 | 접근 방식 | 판정 |
|---|---|---|---|
| 실제 좌석배치도 (항공사별) | **Amadeus Self-Service SeatMap Display API** | 공식 REST API, 무료 테스트 티어. 좌석 좌표·데크 구성·비상구열·날개 위치 반환 | 🟢 유일한 정식 경로 |
| 〃 | Sabre / Travelport SeatMap API | 공식이나 B2B 계약 필요 | 🟡 개인 개발자 접근 난이도 높음 |
| 〃 | AeroLOPA (현 업계 1위, 실측 스케일 도면) | 공개 API 없음. 도면은 저작물 | 🔴 무단 스크래핑 불가 (라이선스 문의는 가능) |
| 〃 | seatmaps.com (B2B 상용 API) | 유료 상용 라이선스 | 🟡 비용 발생 |
| 〃 | SeatMap.app / FlightSeatmap.com | 신흥 대체 서비스, API 미공개 | 🔴 |
| 제조사 "대표" 배치도 | Boeing ACAP PDF / Airbus AC PDF | 무료 공개 PDF 다운로드 | 🟢 단, 실제 운항 배치 아님 |
| 좌석 피치/폭 (편한 좌석) | Amadeus API 일부 + 항공사 공식 페이지 | API + 보완 크롤링 | 🟡 항공사·기체별 편차 큼 |
| 좌석별 소음 | **전용 데이터셋 부재** | 학술 측정 논문 소수 + 물리 기반 추정 | 🔴→🟡 (자체 모델링 필요) |
| 기체 모델별 사고율 | Boeing *Statistical Summary of Commercial Jet Airplane Accidents*, Airbus accidentstats, IATA | 공개 PDF, 수작업 정형화 | 🟢 |
| 사고 원본 데이터 | NTSB 공개 데이터셋(avall.zip) 및 developer.ntsb.gov API, ASN DB | 공식 다운로드/API | 🟢 (단 NTSB는 美 사고 한정) |
| **좌석별 생존율** | 없음 | — | 🔴 **아래 2.4 참조** |

### 2.4 [경고] "좌석별 생존율"은 신뢰할 만한 데이터가 존재하지 않음

- 흔히 인용되는 수치(TIME 2015: 후방 중앙석 치명률 28% vs 중앙부 39% 등)는
  **치명자와 생존자가 함께 발생한 사고만 골라낸 17~35건 규모의 소표본 분석.**
- FAA를 포함한 항공안전 당국·전문가의 공식 입장은 **"가장 안전한 좌석은 없다"** 이다.
  생존 여부는 좌석 위치보다 사고 상황(충돌 각도·화재·수몰·구조 시간)에 압도적으로 좌우됨.
- 유일하게 재현성 있는 발견은 그리니치대 Galea 연구의
  **"비상구에서 5열 이내면 탈출 확률이 유의하게 높다"** 정도. 좌석 단위 생존율이 아니라 **탈출 접근성** 지표.

→ 이 기능을 "생존율 %"로 표기하면 근거 없는 수치를 사실처럼 제시하는 것이 되어
  제품 신뢰도와 법적 리스크 양쪽에 문제가 됨. **재정의 필요 (D-4 참조).**

### 2.5 소음: 통념과 실측이 어긋남

- A321 실측 연구: 1~3열 약 77dB(A) → 7~10열 83dB(A)로 급상승 → 최후열 78dB(A)로 하강.
  즉 "앞이 조용하고 뒤가 시끄럽다"는 통념과 달리 **중간 구간이 가장 시끄러운** 결과.
- 순항 중 객실 소음은 대체로 75~85dB(A) 범위.
- 다만 이는 **특정 기종·특정 편의 소수 측정**이며 전 기종 데이터셋은 존재하지 않음.
- 현실적 대안: 엔진 장착 위치·날개 위치 대비 좌석 상대 위치로 **물리 기반 추정 등급**(정성적)을 산출.
  → 반드시 "추정치"임을 UI에 명시해야 함.

---

### 3. 미결 의사결정 (Open Decisions)

| ID | 결정 사항 | 선택지 | 상태 |
|---|---|---|---|
| D-1 | 앱의 데이터 축 | (a) 제조사→모델 유지 + ACAP 대표배치 / (b) 항공사→기체→배치 로 전환 / (c) 항공편 번호 기준 | ⬛ 무효(폐기) |
| D-2 | 좌석 데이터 조달 경로 | Amadeus API / 상용 라이선스 / 수동 큐레이션(소수 기종) | ⬛ 무효(폐기) |
| D-3 | 소음 정보 산출 방식 | 물리 기반 추정 등급 / 사용자 크라우드소싱 / 기능 제외 | ⬛ 무효(폐기) |
| D-4 | 생존율 기능 재정의 | "생존율" 폐기 후 "비상구 접근성 지표"로 대체 / 사고율만 유지 / 원안 유지 | ⬛ 무효(폐기) |
| D-5 | 플랫폼 | 웹 / iOS / Android / 크로스플랫폼 | ⬛ 무효(폐기) |
| D-6 | 목표 범위 | 개인용 / 공개 출시 / 수익화 | ⬛ 무효(폐기) |
| D-7 | 기체 커버리지 범위 | 전 기종 / 여객기 주력 20~30종 / 국내 취항 기종 한정 | ⬛ 무효(폐기) |

---

### 4. 확정 사항 (Decided)

**D-0. 아이디어 폐기 (2026-08-29)** — 아래 세 가지가 동시에 성립하여 진행하지 않기로 결정.

1. 앱의 근간인 "모델별 기본 좌석배치"가 실재하지 않음 (2.1)
2. 실제 배치 데이터의 무료·합법 조달 경로가 SeatGuru 폐쇄로 사실상 소멸 (2.2, 2.3)
3. 핵심 셀링포인트인 "좌석별 생존율"이 근거 없는 수치 (2.4)

> 살릴 수 있었던 부분: **기종별 사고율**은 Boeing/Airbus/NTSB 공개 데이터로 무료 확보 가능했음.
> 나중에 다른 맥락에서 필요해지면 6장 링크부터 보면 됨.

---

### 5. 리스크 목록

- **R-1 (높음)** 좌석배치 데이터 라이선스. AeroLOPA 등의 도면 무단 사용은 저작권 침해. 스크래핑은 ToS 위반 소지.
- **R-2 (높음)** 근거 없는 안전 수치 제시로 인한 신뢰도 훼손 및 법적 책임. (2.4)
- **R-3 (중간)** Amadeus 무료 티어의 호출 제한 및 상용 전환 시 비용.
- **R-4 (중간)** 항공사 좌석 배치는 수시 변경(리트로핏)되므로 데이터 신선도 유지 비용 지속 발생.
- **R-5 (낮음)** 사용자 이미지 업로드 오버레이 기능은 좌표계·축척 정합이 어려워 실사용성 낮을 가능성.

---

### 6. 참고 링크

- Amadeus SeatMap Display API — https://developers.amadeus.com/self-service/category/flights/api-doc/seatmap-display
- Boeing ACAP (예: 737 MAX) — https://www.boeing.com/content/dam/boeing/boeingdotcom/commercial/airports/acaps/737MAX_RevC.pdf
- Airbus Accident Statistics — https://accidentstats.airbus.com/hull-losses/
- NTSB 항공 데이터 — https://data.ntsb.gov/avdata · https://developer.ntsb.gov/
- ASN Accident Data (Flight Safety Foundation) — https://flightsafety.org/toolkits-resources/asn-accident-data-2/
- AeroLOPA — https://www.aerolopa.com/
- SeatGuru 폐쇄 관련 — https://awardfares.com/blog/seatguru-alternatives/

---

## 아이디어 #2 — 반려동물 통합 앱 (Android)  💬논의중

> 시작: 2026-08-29 · 상태: **데이터 소스 검증 완료 / 구조 설계 논의중**

### 1. 만들려는 것 (사용자 구상)

반려동물 관련 정보를 **지도 + 동(洞) 단위 행정구역 검색**으로 통합 제공하는 안드로이드 앱.

| # | 메뉴 |
|---|---|
| 1 | 미용시설 현황 |
| 2 | 동물병원 현황 |
| 3 | 출입가능 식당 |
| 4 | 동반여행 가능 지역기반 관광정보 |
| 5 | 동물구조단체 현황 |
| 6 | 동물구조단체 반려동물 분양현황 |
| 7 | 야생동물 구조데이터 |

검토 요청: 메뉴별 데이터 API 확보 가능 여부 / 단일 앱 통합 vs 성격별 분할

---

### 2. 메뉴별 데이터 소스 검증 결과 (2026-08-29)

| # | 메뉴 | 소스 | 형태 | 판정 |
|---|---|---|---|---|
| 1 | 미용시설 | LOCALDATA 지방행정인허가 `동물미용업` | 오픈API + 월 전체분 다운로드 | 🟢 |
| 2 | 동물병원 | LOCALDATA `동물병원` / 행안부 동물병원 조회서비스(15154952) | 오픈API | 🟢 |
| 3 | 출입가능 식당 | **식약처 반려동물 동반출입 음식점** (2026-03-01 시행) | Excel/PDF만, **API 없음** | 🟡 |
| 3b | 〃 보완 | 한국문화정보원 반려동물 동반가능 문화시설(15111389) | 파일데이터 → 오픈API 자동변환 제공 | 🟢 |
| 4 | 동반여행 관광정보 | 한국관광공사 TourAPI 반려동물 동반여행 | 오픈API | 🟢 |
| 5 | 구조단체 현황 | 농림축산검역본부 **동물보호센터 정보**(15035887) | 오픈API | 🟢 |
| 6 | 분양현황 | 농림축산검역본부 **구조동물 조회**(15098931, 구 유기동물조회 15001096) | 오픈API, 일 단위 갱신 | 🟢 |
| 7 | 야생동물 | **전국야생동물구조센터정보 표준데이터**(15025453) | 표준데이터(좌표 포함) | 🟢 |

**전부 무료 공개 데이터. 아이디어 #1과 정반대로 데이터 리스크가 거의 없음.**

#### 2.1 [신규] 식약처 반려동물 동반출입 음식점 제도

- 식품위생법 시행규칙 개정(2026-01-02 공포, **2026-03-01 시행**)으로 신설된 신규 제도.
- 현재 등록 **전국 2,636개소** (서울 366). 시행 5개월차라 절대량이 적음.
- 제공 방식: 웹 표 + Excel/PDF 다운로드. **오픈API 없음** → 정기 다운로드 후 파싱하는 ETL 필요.
- ⚠️ **이 목록은 완전하지 않다.** 사이트 명시: "현황에 포함되지 않은 영업자도 시설기준·준수사항을 지켰다면 동반 출입 가능."
  → 즉 공식 목록은 *일부 정답*일 뿐, 없는 식당이 불가능하다는 뜻이 아님. UI 문구에 반영 필요.
- 신규 제도이므로 등록 업소는 계속 증가 예정 → 시간이 갈수록 데이터 가치 상승.

#### 2.2 [주의] 5·6번은 "민간 구조단체"를 포함하지 않는다

- 공공 API가 다루는 범위는 **지자체가 지정한 동물보호센터**.
- 케어·카라 등 **민간 동물권 단체의 자체 구조·분양 동물은 공공데이터에 없음.**
- 즉 메뉴 5·6을 "동물구조단체"라 부르면 실제 내용(=지자체 보호센터)과 어긋남. 명칭 재정의 필요.
- 민간 단체까지 넣으려면 개별 제휴 또는 크롤링 → 아이디어 #1의 R-1과 동일한 리스크 재발.

---

### 3. [최중요] 기술적 관문 — "동 단위 검색"

사용자 요구사항 중 **"동 단위 행정구역 검색"이 이 앱의 아키텍처를 결정한다.**

#### 3.1 어떤 API도 동 단위 필터를 지원하지 않는다

| API | 지원 최소 단위 |
|---|---|
| 국가동물보호정보시스템 | 시도 → 시군구 → 보호소 |
| LOCALDATA | 시도 → 시군구 |
| TourAPI | areaCode → sigunguCode |

→ 셋 다 **시군구까지가 한계.** 동 단위는 어떤 API도 파라미터로 제공하지 않음.

#### 3.2 따라서 앱이 API를 직접 호출하는 구조는 성립하지 않는다

요구사항을 만족시키려면:

1. 전체 데이터를 **서버에 미리 적재(ETL)** 하고
2. 주소 문자열 파싱 또는 좌표 → **법정동/행정동 코드로 직접 인덱싱**한 뒤
3. 앱은 **자체 백엔드만** 호출한다

→ **백엔드 서버 + 배치 파이프라인이 선택이 아니라 필수.** (D-11)

#### 3.3 지역코드 체계가 API마다 전부 다르다

- 국가동물보호정보시스템: 자체 시도코드(`upr_cd`) / 시군구코드(`org_cd`)
- LOCALDATA: 자체 지역코드 + 업종코드(`opnSvcId`)
- TourAPI: 한국관광공사 자체 `areaCode` / `sigunguCode`
- 표준데이터·파일데이터: 법정동코드 또는 주소 문자열만

→ **4개의 서로 다른 코드체계를 법정동코드로 정규화하는 매핑 테이블이 이 앱의 첫 번째 작업이자 핵심 자산.**
→ 한 번 만들어두면 이후 어떤 공공 API를 추가해도 재사용됨.

#### 3.4 기타 실무 함정

- **좌표계**: LOCALDATA 좌표는 WGS84가 아닌 TM 계열(EPSG:5174 추정)로 알려져 있음 → 변환 필요. **실제 응답으로 검증할 것.**
- **폐업 데이터**: LOCALDATA는 폐업·휴업 업소를 그대로 포함 → `상세영업상태코드` 필터 필수. 안 하면 없어진 병원이 지도에 뜸.
- **좌표 결측**: 일부 데이터는 주소만 있고 좌표 없음 → 지오코딩(카카오 로컬 API 등) 보완 필요.

---

### 4. [중요] 카카오맵 무료 쿼터 정책 변경 — 앱 분할 판단에 직접 영향

- **2026-07-21 시행**: 카카오맵 API 무료 쿼터는 **개발자 계정 기준 "첫 번째로 활성화한 앱" 1개에만** 제공.
- 두 번째 앱부터는 유료(비즈월렛 연결). 경로조회 건당 10원, 정적지도 건당 2원.
- 기준일 이전부터 쓰던 앱은 기존 쿼터 유지.

→ **앱을 3개로 쪼개면 2개는 지도 API가 곧바로 유료가 된다.** 분할의 직접 비용. (D-12)

---

### 5. 미결 의사결정 (Open Decisions)

| ID | 결정 사항 | 선택지 | 상태 |
|---|---|---|---|
| D-10 | 앱 분할 여부 | 단일 통합 / 성격별 2~3개 분할 | ⬜ 미정 (권고안 §6) |
| D-11 | 백엔드 구성 | 자체 서버+DB / BaaS(Supabase·Firebase) / 서버리스+정적 배포 | ⬜ 미정 |
| D-12 | 지도 SDK | 카카오맵 / 네이버지도 / Google Maps | ⬜ 미정 |
| D-13 | 메뉴 7 처리 | **구조센터 위치(POI)만** / ~~통계 포함~~ / ~~제외~~ | ✅ 결정 (D-20) |
| D-14 | 메뉴 5·6 범위 | 지자체 보호센터 한정 / 민간단체 포함(제휴 필요) | ⬜ 미정 |
| D-15 | 안드로이드 기술스택 | Kotlin+Compose / Flutter / RN | ⬜ 미정 |
| D-16 | 목표 범위 | ~~개인용~~ / **Play스토어 출시** / 수익화(미정) | ✅ 결정 (D-21) |
| D-17 | 차별화 포인트 | 포인핸드·와요 등 기존 앱 대비 무엇으로 이길 것인가 | ⬜ 미정 |

---

### 6. 리스크 목록

- **R-10 (중간)** 메뉴 6(분양)은 포인핸드·펫버디 등 동일 공공API 기반 경쟁 앱이 이미 다수. 차별화 없으면 무의미.
- **R-11 (중간)** 식약처 동반출입 음식점 API 부재 → 수동 ETL 운영 부담 지속.
- **R-12 (중간)** 공공 API 장애·스펙 변경 시 앱 전체 영향. 서버 캐싱으로 완충 필요.
- **R-13 (낮음)** 카카오맵 쿼터 초과 시 과금. 서버 캐싱으로 지도 API 호출 최소화 설계 필요.
- **R-14 (낮음)** 데이터 정확성(폐업·오등록) 관련 사용자 불만. 출처·갱신일 명시로 완화.

---

### 7. 참고 링크 (아이디어 #2)

- 국가동물보호정보시스템 구조동물 조회 — https://www.data.go.kr/data/15098931/openapi.do
- 동물보호센터 정보 — https://www.data.go.kr/data/15035887/openapi.do
- LOCALDATA API 이용가이드 — https://www.localdata.go.kr/devcenter/apiGuide.do?menuNo=20002
- 행안부 동물병원 조회서비스 — https://www.data.go.kr/data/15154952/openapi.do
- 식약처 반려동물 동반출입 음식점 현황 — https://www.foodsafetykorea.go.kr/portal/petKorea.do
- 반려동물 동반가능 문화시설 위치데이터 — https://www.data.go.kr/data/15111389/fileData.do
- 전국야생동물구조센터정보 표준데이터 — https://www.data.go.kr/data/15025453/standard.do
- TourAPI (한국관광공사 콘텐츠랩) — https://api.visitkorea.or.kr/
- 카카오맵 쿼터 정책 변경 공지 — https://devtalk.kakao.com/t/api-notice-on-new-kakao-map-api-features-and-free-quota-policy/150222

---

### 8. 확정 사항 (Decided) — 2026-08-29

**D-20. 메뉴 7 재정의 — "야생동물 구조 통계" → "야생동물 구조센터 위치(POI)"**
- 야생동물 구조 *통계*는 반려동물 보호자와 사용자 페르소나가 불일치. 제외.
- 대신 "다친 야생동물 발견 시 어디로 신고·이송하나"는 실수요가 있으며,
  전국야생동물구조센터정보 표준데이터는 **좌표를 포함한 POI 구조**라 메뉴 1~4와 동일하게 처리 가능.
- → **[장소] 탭에 레이어 하나 추가하는 형태로 구현.** 추가 개발비용 거의 0.

**D-21. 사용자 프로필 및 목표**
- 목표: **Google Play 스토어 출시** (개인용 아님)
- 개발 경험: **없음** (앱 개발 경험 전무)
- → 기술 선택 기준이 "최적"이 아니라 **"학습자료가 가장 많고 스스로 막힌 걸 풀 수 있는 것"** 으로 바뀜. (§10)

**D-22. 개발 순서 — 7개 전부 만들되 하나씩 붙인다**
- 사용자 결정. 올바른 접근. 단 **붙이는 순서는 "재사용도 높은 순"** 이어야 함. (§11)

---

### 9. [검증 완료] API 최신성 — 2026-08-29 기준

> "API가 존재한다"와 "데이터가 살아있다"는 다른 문제. 전 소스 갱신 상태 실측.

| 소스 | 등록일 | 최종수정 | 갱신주기 | 판정 |
|---|---|---|---|---|
| **LOCALDATA** (미용·병원) | — | 상시 | **월~토 매일 19시 갱신, D-2 기준**<br>전체분은 매월 2일 배포 | 🟢🟢 최상 |
| 국가동물보호정보시스템 구조동물 조회(15098931) | 2022-02-11 | **2026-01-23** | 일 단위 | 🟢 활용신청 2,322건 |
| 동물보호센터 정보(15035887) | — | — | — | 🟢 `_v2` 엔드포인트로 버전업 유지<br>`apis.data.go.kr/1543061/animalShelterSrvc_v2/shelterInfo_v2`<br>기본 트래픽 1,000건/일 (증량 신청 가능) |
| 행안부 동물병원 조회서비스(15154952) | **2025-11-27** | **2026-04-15** | — | 🟢 최근 신설된 신규 서비스 |
| 식약처 반려동물 동반출입 음식점 | 2026-03-01 시행 | 실시간 | **실시간 DB** | 🟢 데이터는 최신 / 🔴 API 없음 |
| TourAPI 반려동물 동반여행 | — | **2026-02-10** | — | 🟢 |
| 전국야생동물구조센터 표준데이터(15025453) | — | 2025-12-18 | **연 1회** (매월 초 기관별 병합) | 🟡 갱신 느리나 구조센터는 거의 불변 → 실사용 무방 |
| 반려동물 동반가능 문화시설(15111389) | 2022-12-29 | **2025-08-14** | **수시 (1회성 데이터)** · 70,650건 | 🟡 **사실상 갱신 중단** — §9.1 |

#### 9.1 [정정] 문화시설 데이터(15111389) 등급 하향 🟢 → 🟡

- 앞서 🟢로 평가했으나 실측 결과 갱신주기가 **"수시(1회성 데이터)"**, 최종수정 **2025-08-14(약 1년 전)**.
- 건수는 70,650건으로 많지만 **지속 갱신되지 않는 스냅샷**.
- → 메뉴 3의 *주* 소스로 쓰면 안 됨. **식약처 실시간 데이터가 주, 이 데이터는 보조**로 배치.
- → UI에 데이터 출처별 **기준일 표기 필수.**

#### 9.2 결론

**메뉴 1·2·6은 일 단위로 살아있고, 3은 실시간이며, 4는 반년 내 갱신됨.**
갱신 리스크가 있는 건 7(연 1회, 무방)과 3b(중단, 보조용)뿐. **데이터 신선도 문제 없음.**

#### 9.3 [수확] TourAPI가 법정동코드 정보를 제공한다

- TourAPI 개방 데이터 15종에 **`법정동코드정보`** 가 포함됨.
- §3.3의 "지역코드 정규화" 작업에 바로 쓸 수 있는 기준 테이블. 별도 수집 불필요.

---

### 10. [신규] 출시 요건 — 개인 개발자 필수 관문

**Google Play 개인 개발자 계정은 프로덕션 출시 전 비공개 테스트가 의무.**

- 2023-11-13 이후 생성된 **개인** 개발자 계정 대상 (2026년 현재 유효)
- 요건: **테스터 12명 이상**이 **14일 연속 opt-in 상태 유지**
- 충족 후 Play Console에서 *프로덕션 액세스* 신청 → 승인되어야 배포 가능
- **사업자 계정은 면제** (D-U-N-S 번호 등록 + 사업자 검증 시 바로 프로덕션 트랙)
- 개발자 등록비 $25 (1회성)

→ ⚠️ **테스터 12명 확보는 개발이 아니라 사람 문제.** 완성 후에 알면 최소 2주가 그냥 날아감.
→ **지금부터 12명을 미리 모아둘 것.** (D-23 대상)
→ 그 외 필수: 개인정보처리방침 URL, 앱 콘텐츠 등급 설문, 데이터 안전 섹션, 타겟 API 레벨 준수.

---

### 11. [권고] 개발 순서 — 재사용도 높은 순

> 사용자 결정(D-22)에 따른 구체적 순서. 원칙: **"앞에서 만든 걸 최대한 재활용하는 순서"**

| 단계 | 대상 | 새로 만드는 것 | 재사용 | 난이도 |
|---|---|---|---|---|
| **0** | **지역코드 정규화 테이블** | 법정동코드 기준 매핑 (TourAPI 법정동코드정보 활용) | — | 🔴 핵심 자산 |
| **1** | **동물병원** (메뉴 2) | 지도·동단위검색·목록·상세 **전 구조** | — | 🔴 가장 힘듦 |
| 2 | 미용시설 (메뉴 1) | **업종코드만 변경** | 1단계 100% | 🟢 거의 공짜 |
| 3 | 야생동물구조센터 (메뉴 7) | 데이터 소스만 교체 | 1단계 100% | 🟢 |
| 4 | 동반 식당 (메뉴 3) | 식약처 Excel ETL 파이프라인 | 1단계 UI 재사용 | 🟡 |
| 5 | 관광정보 (메뉴 4) | TourAPI 연동 + 이미지 처리 | 1단계 UI 재사용 | 🟡 |
| 6 | 보호센터·입양 (메뉴 5·6) | **[입양] 탭 신규** — 사진 중심 목록·상세 | 지역필터만 재사용 | 🔴 |

- **1단계에 동물병원을 두는 이유**: 데이터 품질이 가장 좋고(일 단위 갱신·좌표 포함), 수요가 명확하며,
  여기서 지도·검색·목록·상세라는 앱의 **뼈대 전체**를 완성하게 되기 때문.
- **2·3단계는 검증 장치**: 여기서 "정말 코드가 재사용되는가"가 드러남. 안 되면 1단계 설계를 고쳐야 함.
- 6단계를 마지막에 두는 이유: 화면 구조가 유일하게 다르고, 경쟁(포인핸드)이 가장 치열한 영역이라 학습 후 진입이 유리.

---

### 12. 미결 의사결정 (갱신)

| ID | 결정 사항 | 상태 |
|---|---|---|
| D-10 | 앱 분할 여부 → **단일 통합** | ✅ 결정 (D-24로 확정) |
| D-11 | 백엔드 구성 | ⬜ §13 권고 참조 |
| D-12 | 지도 SDK | ⬜ §13 권고 참조 |
| D-14 | 메뉴 5·6 범위 (민간단체 포함 여부) | ⬜ 미정 |
| D-15 | 안드로이드 기술스택 | ⬜ §13 권고 참조 |
| D-17 | 차별화 포인트 | ✅ 결정 (D-24) |
| D-23 | 클로즈드 테스트 테스터 12명 확보 방안 | ⬜ 미정 — 조기 착수 필요 |

---

### 13. [확정] D-24. 차별화 포인트 — "여행/낯선 지역" 컨텍스트

**사용자의 실제 계기 (2026-08-29):**
> "반려동물과 여행을 가거나 식당을 가거나 동물병원을 찾을 때 불편했고,
> 실제 각각의 앱은 있는데 통합 앱이 없어서 앱을 여러 개 설치해야 했다."

#### 13.1 이것이 D-10(단일 통합)을 확정시킨다

앱을 나누면 **사용자가 겪은 문제를 앱 개발자가 그대로 재현하는 것**이 됨. 분할 논의 종료.

#### 13.2 다만 "통합"만으로는 약하다 — 이기는 지점을 특정해야 함

- 일반적으로 **통합 앱은 각 영역에서 전문 앱보다 얕다.** "얕게 다 있다"가 "깊게 하나 있다"를 이기려면
  **특정 상황 하나에서 확실히 이겨야 한다.**
- 이 앱이 이기는 상황: **"반려동물과 낯선 지역에 간다."**
  - 이때만은 병원·식당·관광지·미용이 **하나의 맥락(같은 지역, 같은 날, 같은 동물)** 으로 묶인다.
  - 전문 앱 3개를 번갈아 여는 것이 가장 고통스러운 순간이기도 함.
- → **포지셔닝: "반려동물 동반 여행/외출 준비 앱".** 각 기능의 단순 나열이 아님.

#### 13.3 [중요] 설계 함의 — "내 주변"이 아니라 "목적지"

- 기존 앱 대부분은 **현재 위치 기반("내 주변")** 이다. 이는 *이미 그곳에 있는* 사용자를 전제한다.
- 그러나 여행 준비는 **집에서, 아직 가지 않은 지역을** 미리 보는 행위다.
- → **지역을 직접 선택해 조회하는 것이 부가 기능이 아니라 기본 동작이어야 한다.**
- → 사용자가 최초 구상부터 요구한 **"동 단위 행정구역 검색"은 바로 이 지점에서 정당화된다.**
  단순 필터가 아니라 **이 앱의 정체성.** (§3의 백엔드 필수 결론도 여기서 재확인됨)

#### 13.4 자연스럽게 도출되는 확장 기능 (후순위, 기록만)

- **여행 묶음**: 지역 하나를 고르고 그 안의 병원·식당·관광지를 한 세트로 저장
- **응급 모드**: 낯선 지역에서 24시간 동물병원 최단 거리 안내 (여행 중 가장 절박한 순간)
- → 둘 다 §11의 1~5단계가 끝난 뒤 고려. **지금 만들지 않는다.**

---

### 14. 미결 의사결정 (갱신 2)

| ID | 결정 사항 | 상태 |
|---|---|---|
| D-11 | 백엔드 구성 → **Supabase + GitHub Actions ETL** 권고 | 🟡 권고안 제시, 확인 대기 |
| D-12 | 지도 SDK → **카카오맵** 권고 | 🟡 권고안 제시, 확인 대기 |
| D-14 | 메뉴 5·6 범위 (민간단체 포함 여부) | ⬜ 미정 (6단계에서 결정해도 무방) |
| D-15 | 기술스택 → **Kotlin + Jetpack Compose** 권고 | 🟡 권고안 제시, 확인 대기 |
| D-23 | 클로즈드 테스트 테스터 12명 확보 | ⬜ **조기 착수 필요** |

---

### 15. [확정] 기술 스택 — D-25 (2026-08-29)

사용자 요청으로 착수용 문서 3종 작성. 이에 따라 D-11·D-12·D-15 확정.

| ID | 항목 | 확정 |
|---|---|---|
| D-15 | 앱 | **Kotlin + Jetpack Compose (Material 3)**, MVVM+Repository, minSdk 26 |
| D-11 | 백엔드 | **Supabase** (PostgreSQL + PostGIS + PostgREST). 자체 서버 없음 |
| D-11b | ETL | **Python + GitHub Actions cron**. 서버 없음 |
| D-12 | 지도 | **Kakao Map Android SDK v2** |
| D-25 | 디자인 | Material 3 기반 **"차분한 안내자"** 컨셉. Primary `#2E6B4F`, 다크모드 필수 |

**선택 기준**: "최적"이 아니라 **"막혔을 때 스스로 풀 수 있는가"**.
배운 것을 Kotlin/Compose와 Python 둘로 한정하기 위해 Hilt(초기)·Flutter/RN·자체서버·GraphQL·멀티모듈을 배제.

**추가 결정 (문서 작성 중 도출)**
- **D-26. `places` POI 통합 테이블** — 메뉴 1·2·3·4·7을 한 테이블로 통합.
  §11 재사용 전략의 DB 구현. 지도·목록·상세 화면 코드를 1벌만 만들면 됨.
- **D-27. MVP 우선 출시** — 3단계 완료 시점에 비공개 테스트 트랙 배포.
  테스터 12명×14일 요건 때문에 완성 후 테스트하면 2주가 순수 대기로 소모됨.
  → 4~6단계 개발 중 테스트 기간을 병행 소진.
- **D-28. "데이터 없음" ≠ "불러오기 실패"** — 두 상태를 절대 같은 화면으로 처리하지 않음.
  사용자가 "이 동네엔 병원이 없구나"로 오해하면 앱 신뢰가 무너짐.

---

### 16. 산출 문서

| 문서 | 역할 |
|---|---|
| `개발계획서.md` | 배경·목표·포지셔닝·범위·리스크·출시 전략 (WHY & WHAT) |
| `spec.md` | 기술 스택·아키텍처·데이터 모델·API·화면·디자인 (WHAT in detail) |
| `plan.md` | 단계별 작업·완료 기준·체크리스트·일정 (HOW & WHEN) |
| `DECISIONS.md` | (본 문서) 검토 과정과 의사결정 이력 |

### 17. 남은 미결

| ID | 내용 | 비고 |
|---|---|---|
| D-14 | 메뉴 5·6에 민간 동물보호단체 포함 여부 | 6단계에서 결정해도 무방 |
| D-23 | **테스터 12명 확보** | ⚠️ 개발과 무관하게 즉시 착수 필요 |
| — | 앱 이름 미정 | 스토어 등록 전까지 |

---

### 18. [확정] D-29. 지역 기준 시점 — 광역시도 16개 (2026-08-30 실측)

0단계 `regions` 적재 결과 **광역시도가 17개가 아니라 16개**로 나왔다. 누락이 아니라 실제 행정구역 개편이 반영된 것이다.

| 변경 | 옛 코드 | 새 코드 |
|---|---|---|
| 광주광역시 + 전라남도 → **전남광주통합특별시** | 29 / 46 | **12** |
| 강원도 → 강원특별자치도 | 42 | 51 |
| 전라북도 → 전북특별자치도 | 45 | 52 |

적재 결과: 시도 16 · 시군구 256 · 읍면동 5,067.

#### 18.1 기준 고정

- `regions` 는 **최신 법정동코드 파일 시점**을 기준으로 한다. 행정동이 아니라 **법정동** 기준이다. (plan.md 0단계 경고 사항)
- 통합·개편으로 사라진 옛 코드(29·46·42·45)는 파일에서 `폐지` 이므로 적재하지 않는다.

#### 18.2 [중요] 이것이 매핑 단계의 핵심 리스크다

공공 API 들이 **같은 속도로 개편을 반영하지 않는다.** LOCALDATA·국가동물보호정보시스템·TourAPI 가 당분간 옛 코드(광주 29 / 전남 46)를 그대로 쓸 가능성이 높다.

→ 즉 `localdata_cd` · `apms_org_cd` · `tour_area_cd` 매핑에서 **새 코드 12xx ↔ 옛 코드 29xx·46xx 대응을 수동으로 이어줘야 하는 구간이 생긴다.**
→ `regions` 에 외부 코드 매핑 컬럼을 둔 설계(D-26 이전, spec.md §3.1)가 정당화되는 지점이다. 앱은 새 코드만 알면 되고, 낡은 코드는 이 테이블이 흡수한다.

#### 18.3 매핑 ETL 의 필수 요건

- 매핑 후 **미매핑 지역 목록을 반드시 출력**한다. 조용히 NULL 로 남기지 않는다.
- 미매핑이 0이 될 때까지 0단계를 완료로 보지 않는다. (plan.md 0단계 DoD)
- 통합 지역은 **1:N 대응**(옛 2개 → 새 1개)이 되므로 단순 1:1 조인으로 처리하면 데이터가 절반 누락된다.

---

### 19. [확정] D-30. 인천 자치구 개편 — D-29 와 같은 유형의 두 번째 사례 (2026-09-02 실측)

매핑 ETL 을 붙이면서 인천에서도 같은 문제가 나왔다. **D-29 가 광주·전남만의 예외가 아니라는 뜻이다.**

| 개편 | `regions`(법정동) | APMS | TourAPI |
|---|---|---|---|
| 중구 + 동구 → **제물포구** | 반영 | 반영 | ❌ 아직 `중구`/`동구` |
| 중구 영종도 → **영종구** | 반영 | 반영 | ❌ 아직 `중구` |
| 서구 → **서해구** + **검단구** | 반영 | 반영 | ❌ 아직 `서구` |

→ **APMS 는 개편을 따라왔고 TourAPI 만 뒤처져 있다.** 즉 "공공 API 는 다 늦다"가 아니라
**소스마다 반영 속도가 다르다.** 앞으로 소스를 추가할 때마다 시도·시군구 목록을 받아
`regions` 와 대조하는 절차를 반드시 넣는다.

#### 19.1 대응 — 규칙이 아니라 예외표

통합·분할은 규칙으로 유도할 수 없다. `etl/petetl/sources/mapping.py` 에 예외표 두 개를 둔다.

```python
TOUR_SIDO_ALIAS    = {"전남광주통합특별시": ("광주", "전라남도")}          # D-29
TOUR_SIGUNGU_ALIAS = {("인천광역시", "제물포구"): "중구", ...}            # D-30
```

TourAPI 가 개편을 반영하면 **이 표에서 해당 줄을 지우기만 하면 된다.** 로직은 손대지 않는다.

---

### 20. [중요] D-31. TourAPI 는 코드와 주소의 기준 시점이 다르다 (2026-09-02 실측)

교차검증 중 발견. `areaBasedList2` 를 옛 코드로 부르면 **주소는 새 행정구역명으로 돌아온다.**

```
area=2, sigungu=10 (옛 '인천 중구')  →  addr1: "인천광역시 제물포구 월미로 33"
                                        addr1: "인천광역시 영종구 선녀바위로 68-2"
area=31, sigungu=12 ('성남시')      →  addr1: "경기도 성남시 분당구 ..."
                                        addr1: "경기도 성남시 수정구 ..."
```

즉 **지역코드는 옛 체계, 주소 문자열은 새 체계**인 혼종 상태다.

#### 20.1 5단계(관광정보) ETL 설계에 직접 영향

- 코드로 조회하면 **옛 구 단위까지밖에 못 좁힌다.** 제물포구와 영종구가 한 덩어리로 나온다.
- 따라서 TourAPI POI 를 `region_code` 에 배정할 때는 **반드시 `addr1` 문자열을 파싱**해야 한다.
  코드만 믿고 시군구를 정하면 인천·성남 같은 곳에서 전부 틀린다.
- 같은 이유로 **일반구(성남시 분당구 등)는 코드로는 영원히 구분할 수 없다.** 주소 파싱이 유일한 길이다.

> 이 성질은 오히려 유리하다. `regions.full_name` 이 새 체계이므로 주소 문자열과 직접 대조된다.

---

### 21. 0단계 매핑 실측 결과 (2026-09-02)

`python run.py mapping` — **미매핑 0건.** plan.md 0단계 DoD 중 매핑 항목 충족.

| 컬럼 | 채워진 행 | 비고 |
|---|---|---|
| `apms_upr_cd` | 5,339 / 5,339 | 전부 |
| `apms_org_cd` | 5,289 | 누락 50 = 시도 16 + 세종 시군구 1 + 세종 읍면동 33 |
| `tour_area_cd` | 5,338 | 누락 1 = 전남광주통합특별시 **시도 행**(코드가 2개라 의도적으로 비움) |
| `tour_sigungu_cd` | 5,323 | 누락 16 = 시도 행 전부 (시군구 코드가 없는 레벨) |

**세종특별자치시의 `apms_org_cd` 가 NULL 인 것은 누락이 아니다.** 세종은 시군구가 없어
APMS 하위 목록 자체가 0건이다. 미매핑으로 세지 않는다.

**전남광주통합특별시 시도 행의 `tour_area_cd` 를 비운 이유**: TourAPI 코드가 `5`+`38` 둘이라
TEXT 한 칸에 담을 수 없다. 앱은 시군구 단위로만 외부 API 를 부르므로 실사용에 문제가 없고,
시군구 27개 행에서는 광주 5구 → `5`, 전남 22개 → `38` 로 정확히 갈렸다.

#### 21.1 매핑이 1:1 이 아닌 세 가지 이유

1. **일반구** — `regions` 는 '성남시 분당구'까지 있고 두 API 는 '성남시'까지만 있다 → **N:1**.
   경기도만 봐도 regions 47 · APMS 33 · TourAPI 31.
2. **개편 반영 속도 차** — D-29 / D-30.
3. **쓰레기 행** — APMS 경상남도 하위에 `orgCd=6489999` 처럼 `orgdownNm` 이 아예 없는 행이 있다.

#### 21.2 인증키 운영 메모

- 공공데이터포털은 **계정당 인증키 1개**다. 데이터별 '활용신청'을 걸면 그 키로 전부 열린다.
  → `.env` 는 `APMS_API_KEY`/`TOUR_API_KEY` 를 나누지 않고 **`DATA_GO_KR_KEY` 하나**로 둔다.
- **Encoding 키를 넣으면 실패한다.** requests 가 한 번 더 인코딩해서 `%2F` 가 `%252F` 가 되고
  `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` 로 떨어진다. base64 에는 `%` 가 없으므로
  `config.normalize_service_key()` 가 `%` 를 보면 Encoding 키로 판정해 자동으로 되돌린다.
- **동물보호센터(`shelterInfo_v2`)는 별도 활용신청이 필요하다.** 구조동물(15098931)과 다른
  데이터셋이다. 6단계 착수 시 신청한다.
- TourAPI 엔드포인트가 `KorService1` → **`KorService2`** 로 바뀌었다. 시군구 목록은 별도
  오퍼레이션이 아니라 `areaCode2` 에 `areaCode` 를 넘겨서 받는다(`sigunguCode2` 는 없다).

---

### 22. 미결 의사결정 (갱신 3)

| ID | 내용 | 상태 |
|---|---|---|
| D-14 | 메뉴 5·6에 민간 동물보호단체 포함 여부 | ⬜ 6단계에서 결정 |
| D-23 | **테스터 12명 확보** | ⬜ ⚠️ 여전히 미착수. 리드타임 최장 |
| D-32 | `localdata_cd` 매핑 | ⬜ **LOCALDATA 인증키 대기** (별도 사이트) |
| D-33 | 읍면동 중심좌표 확보 방법 → **카카오 로컬 API** | ✅ 결정 (§23). 카카오 REST 키 대기 |
| — | 앱 이름 미정 | 스토어 등록 전까지 |

---

### 23. [확정] D-33. 읍면동 중심좌표는 카카오 로컬 API 로 채운다 (2026-09-02)

> ⚠️ **정정.** 앞서 "중심좌표는 인증키가 필요 없다"고 적었으나 **틀렸다.**
> 키 없이 되는 깔끔한 경로가 없다. 아래 검토 결과를 남긴다.

#### 23.1 검토한 소스

| 소스 | 형태 | 판정 |
|---|---|---|
| 전국법정구역(읍면동)정보표준데이터 (15029173) | Vworld WMS/WFS + shapefile | ❌ 별도 키 + GIS 툴체인(GDAL) 필요 |
| 국토교통부 전국 법정동 (15063424) | 파일데이터(CSV) | ❌ 코드·명칭만. 좌표 없음 |
| 국가공간정보포털 법정구역 경계 | shapefile | ❌ 수동 다운로드 + PostGIS 적재 + `ST_Centroid` |
| SGIS(통계지리정보서비스) 경계 API | REST | 🟡 되지만 **또 하나의 키**가 늘어난다 |
| **카카오 로컬 API** (주소→좌표) | REST | ✅ **채택** |

#### 23.2 카카오를 고른 이유

- **키가 늘지 않는다.** 카카오 REST API 키는 1단계 '좌표 결측분 지오코딩'(spec §2.2)에
  어차피 필요하다. 여기에 하나 더 쓰는 것이므로 새 의존이 아니다.
- **GIS 툴체인이 필요 없다.** shapefile 경로는 GDAL/geopandas 를 끌어들이는데,
  이는 D-25 의 선택 기준("막혔을 때 스스로 풀 수 있는가")에 정면으로 어긋난다.
- 중심좌표의 용도는 **지역 선택 후 지도 초기 위치**다. 행정경계 도심(centroid)만큼
  엄밀할 필요가 없고, 주소 검색 결과의 대표 좌표로 충분하다.

> ⚠️ **카카오 키는 2종이고 서로 다르다.** 지도 SDK 는 **네이티브 앱 키**,
> 이 ETL 은 **REST API 키**를 쓴다. 같은 앱에서 둘 다 발급된다. 바꿔 넣으면 401 이다.

#### 23.3 개편 지역 대응 (D-29 / D-30 재등장)

`regions` 는 `전남광주통합특별시`·`제물포구` 같은 신규 명칭을 쓰는데 카카오 주소 DB 가
이를 반영하지 않았을 수 있다. `coords.query_candidates()` 가 순서대로 되짚는다.

1. `full_name` 그대로
2. 시도를 **옛 이름**으로 (`전남광주통합특별시` → `광주광역시` / `전라남도`)
3. 시군구를 **옛 이름**으로 (`제물포구` → `중구` / `동구`)
4. 시도를 떼고 `시군구 읍면동` 만으로

> 이 표는 `mapping.py` 의 `TOUR_*` 표와 값이 비슷하지만 **목적이 다르다.**
> 저쪽은 'TourAPI 가 쓰는 이름'(`광주`), 이쪽은 '실제 옛 행정구역명'(`광주광역시`)이다.
> 하나로 합치면 둘 중 하나가 반드시 틀어진다.

#### 23.4 안전장치

- **x 가 경도, y 가 위도다.** 카카오 응답은 순서가 뒤집혀 있다. 바꿔 쓰면 좌표가 동해로 간다.
  → `to_latlng()` 가 대한민국 범위(위도 33~39.5 / 경도 124~132)를 벗어나면 버린다.
  → 테스트가 뒤바뀐 값을 명시적으로 거부한다.
- **재실행 안전.** 기본은 `center_lat` 이 비어 있는 행만 처리한다. 끊겨도 이어서 돈다.
- **쿼터 보호.** `--limit N` 으로 한 번에 처리할 개수를 제한한다. 429 는 대기 후 재시도한다.

---

### 24. [확정] D-34. 1단계 동물병원은 LOCALDATA 없이 시작한다 (2026-09-02)

**행안부 동물병원 조회서비스(15154952)** 가 같은 인허가 데이터를 공공데이터포털 오픈API 로
재공개한다. **자동승인 · 개발계정 10,000건/일.** 이미 가진 `DATA_GO_KR_KEY` 에
활용신청만 걸면 된다. 즉 **LOCALDATA 가입 대기가 1단계를 막지 않는다.**

- 다만 **2단계 미용시설은 LOCALDATA 가 필요할 가능성이 크다.** 동물병원·동물판매업은
  오픈API 가 있으나 **동물미용업은 오픈API 가 확인되지 않고 파일데이터로만 보인다.**
  (검색 근거이므로 단정하지 않는다. LOCALDATA 는 어차피 신청해 둔다.)
- **[수확] 좌표계가 확정됐다.** 15154952 문서가 **EPSG:5174**(보정계수 없는 Bessel 중부원점TM)
  로 명시한다. `spec.md` 의 "TM 계열(EPSG:5174 **추정**)" 이 사실로 굳었다.
  plan.md 1단계 리스크 중 하나가 줄었다. 그래도 실제 응답으로 한 번 더 검증한다.
- **트래픽 한도는 데이터셋마다 다르다.** spec.md 에 "기본 1,000건/일"로 적었으나
  15098931·15154952 는 **10,000건/일**이다. 수정했다.

---

### 25. 1단계 착수 — 앱 뼈대 (2026-09-03)

**D-35. applicationId 를 `io.github.junkie300.petapp` 로 임시 확정한다.**

앱 이름이 아직 없다. 그런데 `applicationId` 는 **스토어에 한 번 올리면 영구히 바꿀 수 없고**
플레이 URL(`play.google.com/store/apps/details?id=...`)에 그대로 노출된다.

- 지금 값은 **GitHub 핸들 기반**이라 충돌이 없고, 실제로 소유한 네임스페이스다.
- 소유하지 않은 도메인(`kr.pet.*` 같은)을 역방향으로 쓰는 것은 관례 위반이라 피했다.
- **첫 업로드 전까지는 한 번의 일괄 치환으로 바꿀 수 있다.** 앱 이름이 정해지면 그때 바꾼다.
- 사용자에게 보이는 이름은 `res/values/strings.xml` 의 `app_name` 이며 이건 언제든 바꿀 수 있다.

**D-36. 툴체인은 AGP 8.13.2 · compileSdk 36 으로 고정한다.**

최신 AndroidX(core-ktx 1.19, compose-bom 2026.08, lifecycle 2.11 등)는 **AGP 9.1+ 와
compileSdk 37** 을 요구한다. 반면 이 PC 에 설치된 Android Studio 는 `AI-253`(2025.3)이라
AGP 9 프로젝트를 열지 못한다.

- 새 프로젝트를 최신 라이브러리로 시작하는 것보다 **설치된 IDE 에서 지금 열리는 것**이 우선이다
  (D-25 의 선택 기준 — "막혔을 때 스스로 풀 수 있는가"). AGP 8.x 는 튜토리얼도 압도적으로 많다.
- 그래서 `gradle/libs.versions.toml` 을 AGP 8.13.2 에서 도는 최신 조합으로 내려 고정했다.
  버전을 올리려면 **Android Studio 를 먼저 최신으로 올린 뒤** 카탈로그 한 파일만 고치면 된다.
- ⚠️ 릴리스 빌드에서 `R8: An error occurred when parsing kotlin metadata` 경고가 나온다.
  Kotlin 2.3.21 이 AGP 8.13.2 내장 R8 보다 새로워서 나는 경고다. 빌드는 성공하며,
  kotlinx.serialization 의 serializer 는 `proguard-rules.pro` 의 keep 규칙으로 보호한다.

**D-37. 행안부 동물병원 조회서비스(15154952)의 엔드포인트를 아직 알아내지 못했다.**

D-34 에서 "LOCALDATA 없이 1단계를 시작할 수 있다"고 적었으나, **호출 경로를 특정하지 못했다.**

- data.go.kr 의 오픈API 상세 페이지는 **명세를 로그인 뒤 스웨거로만** 보여준다. 웹 검색·크롤링으로는
  baseURL·오퍼레이션명이 나오지 않는다.
- `apis.data.go.kr/1741000/...` 아래 그럴듯한 경로 4종을 찔러 봤으나 전부
  `NO_OPENAPI_SERVICE_ERROR`(경로 없음)였다. 추측으로 맞출 수 있는 성질이 아니다.
- `https://infuser.odcloud.kr/oas/docs?namespace=15154952/v1` 도 아니다(odcloud 계열이 아님).
- → **활용신청을 하면 마이페이지 > 오픈API > 개발계정 상세에 참고문서(엑셀/한글)와 함께
  엔드포인트가 표시된다.** 그걸 받아야 ETL 을 쓸 수 있다. 신청 자체는 자동승인이라 즉시 끝난다.

**D-38. 앱의 첫 화면은 S-01 지역 선택이다. 지도(S-02)보다 먼저 만든다.**

- S-01 은 **카카오 키가 없어도 완성된다.** 0단계에서 채운 `regions` 만 쓴다.
- 지도·상세는 네이티브 앱 키가 나온 뒤 이 위에 얹는다. 대기 시간을 개발 시간과 겹치기 위함이다.
- 이 단계에서 `spec.md §5.3`(로딩·빈 상태·실패 구분)을 **타입으로 강제**했다 — `UiState`.
  세 상태를 하나로 합치고 싶어지면 `UiState.kt` 주석을 먼저 읽을 것.

---

### 26. 디자인 시안 대조 (2026-09-05)

`이태우_디자인시안/` PNG 5장(홈·지도·상세·여행·입양)을 `spec.md` 및 구현 코드와 대조했다.
**시안은 참고용**이며 확정안이 아니다.

**대조 시점의 매몰 비용** — UI 코드는 총 527줄이고 화면다운 화면은 `RegionPickerScreen.kt`
200줄 하나뿐이었다. 지도·상세·탭바는 아직 없다. **디자인 변경 비용이 가장 싼 시점**이라
지금 대조한 것이 맞다. 비싼 것은 UI 가 아니라 데이터(`regions` 5,339행·매핑·스키마)이며
이쪽은 디자인과 무관하게 그대로 산다.

**일치했던 것** — 시안의 팔레트가 `§6.2` 와 사실상 동일했다(딥그린 `#2E6B4F`,
오렌지 `#E8734A`, 크림 배경). 카테고리 6칸도 `place_category` ENUM 과 1:1 로 붙었다.

**D-39. 하단 탭을 3개에서 4개로 늘리고, 홈 허브(S-00)를 신설한다.**

시안의 정보구조를 받되 포지셔닝은 `spec.md` 를 지키는 절충이다.

- 채택: 홈 허브 · 카테고리 그리드 · 4탭 구조 · 시안의 비주얼 톤
- 거부: **"내 주변 2km" 를 홈의 주인공으로 두는 것.** 시안은 현재 위치 기반으로 열리는데,
  그건 카카오맵·네이버지도가 이미 잘하는 자리이고 **D-24 가 의도적으로 피한 지점**이다.
  이 앱의 기준점은 "내가 고른 지역"이며, 홈 상단 지역 칩이 그것을 항상 보여준다.
- 수정: 시안의 지역 칩은 시군구까지("서울특별시 마포구")였다. → **읍면동까지 표기한다.**
  읍면동 단위가 이 앱의 존재 이유다(`§3` regions). 칩에서 단위를 낮추면 제품이 흐려진다.
- 수정: 시안의 "마이" 탭 → **`더보기`** 로 이름만 바꿔 채택. 로그인 없는 앱이라
  계정 화면이 들어갈 자리가 없다. 즐겨찾기·데이터 출처·앱 정보만 담는다.
- 거부: 큐레이션 섹션("이번 주 함께 가기 좋은 곳") — 매주 사람이 고르는 운영 비용이 든다.
- 수정: 지도 핀을 5색 → **딥그린·오렌지 2색**. 아이콘이 다르므로 `§6.5` 를 위반하지 않는다.
  5색은 라벨이 함께 붙는 필터 칩·배지에서만 계속 쓴다.
- **S-01 은 버리지 않는다.** 홈의 지역 칩에서 여는 화면으로 재사용한다 → 200줄 그대로 산다.

**D-40. 별점·후기·장소 사진은 MVP 에서 넣지 않는다. 사용자가 모인 뒤로 미룬다.**

시안이 약속하지만 **공공데이터에 존재하지 않는** 필드들이다.

| 시안 요소 | 조달 가능 여부 |
|---|---|
| ⭐ 별점 4.7 · "128개의 후기" | ❌ 어떤 소스에도 없다 |
| 장소 사진 | ❌ 관광지(TourAPI `image_url`)만. 식당·병원·미용은 없다 |
| 실내 가능 · 주차 · 화장실 태그 | ◐ 식당 `pet_area` 정도. 주차·화장실은 없다 |
| "추천 코스 2시간" | ❌ 코스 엔티티가 스키마에 없다 |

- 별점·후기를 진짜로 넣으면 **사용자 작성 콘텐츠**가 되고 로그인·신고·차단·운영이
  전부 따라온다. 그래서 시안의 "마이" 탭과 세트였던 것으로 보인다. MVP 로는 무겁다.
- **대체안**: 별점이 있던 자리에 `출처 · 기준일` 을 놓는다. `§5.2` 가 이미 의무 표기로
  요구하고 있고, 여행 준비 도구에서는 "이 정보가 언제 것인가"가 별점보다 신뢰에 직접 기여한다.
- 사용자가 충분히 모이면 그때 다시 꺼낸다 (`spec.md §10`).

**D-41. 「펫어라운드」를 앱 이름으로 쓰지 않는다. D-35 의 `applicationId` 를 유지한다.**

시안 홈 화면의 제목이 「펫어라운드」였고 채택을 검토했으나, **이미 존재하는 서비스다.**

- [petaround.kr](https://www.petaround.kr/petaround) — 김신형 대표의 스타트업.
  소개가 "여행부터 액티비티, 산책까지", "반려동물 종합 여가 플랫폼"이고
  **반려동물 동반 업소를 필터·해시태그로 찾는 기능까지 있다.** 이름도 영역도 정면으로 겹친다.
- `applicationId` 는 **스토어 첫 업로드 후 영구히 바꿀 수 없다**(D-35). 확인 없이 넣었으면
  되돌릴 수 없는 상표 분쟁 소지를 안고 출시할 뻔했다.
- → `io.github.junkie300.petapp` **유지**. 앱 이름은 계속 미정이며,
  `strings.xml` 의 `app_name` 은 임시값이었다 → **D-42 에서 「대반려지도」로 확정.**
- ⚠️ **이름 후보가 나오면 스토어·상표부터 검색한다.** 시안·문서에 적혀 있다는 것은
  그 이름을 쓸 수 있다는 뜻이 아니다.

---

### 27. 앱 이름 확정 (2026-09-05)

**D-42. 앱 이름은 「대반려지도」로 한다. `applicationId` 는 그대로 둔다.**

> ⚠️ **D-43 으로 대체됐다 (2026-09-05).** 아래 내용은 이력으로 남긴다.

후보는 「곁에펫」·「어디펫」·「대반려지도」였고 셋 다 충돌 검색에서 깨끗했다.

**「대반려지도」를 고른 이유**

- **대동여지도의 5음절을 그대로 치환한다** (대동여지도 → 대반려지도). 한국인이라면 학교에서
  배운 이름이라 한 번 듣고 구조가 잡힌다. 설명이 필요 없는 이름이다.
- **이름이 제품의 실제 자산을 말한다.** 대동여지도는 *전국을* 그린 지도다. 이 앱의 자산도
  `regions` **전국 5,339행**이고, 공공데이터 기반이라 **커버리지가 전국에 균일**하다.
  경쟁 서비스 대부분은 사용자 제보 기반이라 수도권만 촘촘하다. 이름이 그 차이를 가리킨다.
- **`펫OO` 무리에서 벗어난다.** 국내 펫 앱 이름이 펫프렌즈·펫피·펫나우·펫썸·펫트워크·
  어바웃펫·우주펫으로 이미 포화다. 「어디펫」은 쉽지만 **이 무리에 그대로 섞여** 검색에서도
  기억에서도 묻힌다. 상표 식별력도 `어디`(기술적) + `펫`(보통명칭) 조합이라 약하다.
- 「곁에펫」은 발음이 [겨테펫]이라 듣고 다시 적기 어렵고, *늘 곁에 있음*이라는 뉘앙스가
  D-24 의 "낯선 지역에 가기 전에 미리" 와 어긋났다. 산책·돌봄 앱 쪽 이름에 가깝다.

**약점을 알고 고른다** — 5음절로 후보 중 가장 길고, 로마자(`daebanryeojido`)가 어색하다.
런처 라벨은 5글자라 잘리지 않으므로 실사용에 지장은 없고, 로마자는 `applicationId` 에
이름을 넣지 않기로 했으므로 비용이 발생하지 않는다.

**바꿀 수 있는 것과 없는 것을 분리해 둔 것이 이 결정의 핵심이다.**

| | 나중에 변경 |
|---|---|
| `applicationId` `io.github.junkie300.petapp` | ❌ 스토어 첫 업로드 후 영구 고정 |
| 스토어 등록정보의 앱 이름 | ✅ 언제든 |
| `strings.xml` 의 `app_name` | ✅ 언제든 |

- `applicationId` 에 이름을 **일부러 넣지 않는다.** 이름과 무관한 값(`petapp`)으로 두는 한
  이름은 언제든 무료로 바꿀 수 있다. D-41 에서 위험했던 것도 이 경계를 넘으려 했기 때문이다.
- ⚠️ **KIPRIS 상표 조회(제9류 소프트웨어·제42류 SaaS)는 스토어 등록 전에 반드시 직접 확인한다.**
  웹 검색으로는 **출원 중인 상표가 잡히지 않는다.** 지금 검색이 깨끗했다는 것은
  "쓰는 서비스가 안 보인다"까지이지 "등록 가능하다"가 아니다.

**[중요] 검색 중 발견한 경쟁 서비스 — 「델고」**

- [델고](https://apps.apple.com/sr/app/id1662427735) — "**반려인들이 함께 만든 강아지 동반 지도**".
  카페·식당·병원·미용실·유치원을 지도에 보여준다. **이 제품의 컨셉과 정면으로 겹친다.**
- 다만 **데이터 조달 방식이 반대다.** 델고는 사용자 제보(UGC), 이쪽은 공공데이터 ETL 이다.
  → UGC 는 초기 밀도가 높은 대신 **지역 편중**이 심하고 폐업 반영이 늦다.
  → 공공데이터는 **전국이 균일하고 기준일이 명확**하지만 별점·사진 같은 질감이 없다(D-40).
- **이것이 D-24 차별점의 실제 근거다.** "낯선 지역"에서 갈라진다 — 사용자가 사는 동네는
  델고가 이미 촘촘하지만, **처음 가는 읍면동은 UGC 가 비어 있다.** 우리는 거기가 채워져 있다.
- → `개발계획서.md` 의 경쟁 분석에 반영할 것. 이 앱을 "동네 지도"로 포지셔닝하면
  델고와 정면 승부가 되고 밀도에서 진다. **"가기 전에 미리"를 계속 붙들어야 한다.**

---

### 28. 앱 이름 재확정 — 「미리펫」 (2026-09-05)

**D-43. 앱 이름은 「미리펫」으로 한다. D-42(대반려지도)를 대체한다.**

D-42 를 정한 당일 사용자가 「대반려지도」의 `반려` 를 문제로 지적했고, 타당해서 바꾼다.

**바꾼 이유 — `반려` 는 사람에게 더 많이 쓰는 말이다.**

- 국어에서 `반려(伴侶)` 의 1차 의미는 **배우자·짝**이다(반려자·인생의 반려).
  `반려동물` 이라는 복합어에서만 동물을 가리키는데, **`반려` 만 떼어 쓰면 사람 쪽으로 읽힌다.**
- 「대반려지도」는 그래서 첫인상이 모호했다. 4음절을 다 읽어야 반려**동물** 앱인 줄 안다.
- `펫/팻` 은 그런 중의성이 없다. **한 음절만으로 대상이 확정된다.**

**「미리펫」을 고른 이유**

- **이름이 제품의 한 줄 정의다.** D-24 의 차별점이 "낯선 지역에 가기 전에 **미리** 확인"인데,
  그 단어가 이름의 앞에 그대로 있다. 설명과 이름이 같은 말을 한다.
- 3음절, 입력·발음·전달이 모두 쉽다. 로마자도 `miripet` 으로 자연스럽다.
- 충돌 검색 결과 깨끗하다. 유사한 이름의 서비스가 없다.

**표기는 `펫` 이다 — 표준 표기를 따른다.**

- 외래어 표기법상 pet 은 **`펫`** 이다. 처음 `팻` 으로 적힌 것은 오타였고 바로 잡았다.
- 비표준 표기(`팻`)가 상표 식별력에는 유리했겠지만, **검색 오타 위험**이 그 이득보다 크다.
  사용자는 표준 표기로 친다. 이름은 기억한 대로 쳤을 때 나와야 한다.
- 대신 `펫` 은 보통명칭처럼 쓰이므로(펫프렌즈·펫피·펫나우·어바웃펫·마이펫·비마이펫·페오펫…)
  **식별력은 앞의 `미리` 가 감당한다.** 상표 출원 시 요부는 `미리` 쪽이다.
- 충돌 검색은 `미리펫`·`미리팻` 두 표기로 함께 돌렸고 둘 다 깨끗했다.

**변경 비용은 0 이었다.** D-41 에서 `applicationId` 에 이름을 넣지 않기로 해 둔 덕분에,
D-42 → D-43 은 `strings.xml` 한 줄과 문서뿐이었다. **이 경계를 지킨 것이 실제로 값을 했다.**

**D-44. 다이내믹 컬러를 끄고 브랜드 팔레트를 기본으로 한다.**

에뮬레이터에 처음 올려 보고 발견했다. 화면이 전부 파란색이었다 — 브랜드 딥그린 `#2E6B4F`
가 어디에도 없었다. `Theme.kt` 의 `dynamicColor = true` 때문이고, `spec.md §6.2`
("Dynamic Color 를 지원하되 브랜드 팔레트를 fallback 으로")를 문자 그대로 구현한 결과였다.

- **fallback 이라는 말이 실제로는 "거의 안 쓰임"을 뜻했다.** minSdk 26 이지만 실사용
  기기는 대부분 Android 12+ 이므로, 배경화면 색이 **항상** 이긴다.
- **지도가 붙으면 충돌한다.** 핀 색(`CategoryColor`)은 하드코딩이라 브랜드 색으로 고정인데
  주변 UI 만 배경화면을 따라간다 → 보라 배경화면 + 초록·주황 핀.
- 이름(D-43)·팔레트까지 정해 둔 마당에 브랜드를 배경화면에 양보할 이유가 없다.
- → `dynamicColor` 기본값을 `false` 로. 파라미터는 남겨 둬 필요하면 켤 수 있게 한다.

⚠️ **"지원한다"와 "기본값으로 쓴다"를 구분하지 않으면 이런 일이 생긴다.**
스펙에 옵션을 적을 때는 **기본값이 무엇인지**까지 적는다.

**D-45. `lightColorScheme` 은 지정하지 않은 토큰을 M3 기본 보라로 남긴다. 뉴트럴 램프를 직접 깐다.**

D-44 로 다이내믹 컬러를 끄자마자 드러났다. 브랜드 딥그린은 나왔는데
**드롭다운 메뉴 배경이 연보라**였다.

- `lightColorScheme(primary = ..., surface = ...)` 처럼 **일부만 넘기면 나머지는 기본값**이다.
  그 기본값이 Material 3 **baseline 보라 팔레트**다.
- 메뉴·카드·바텀시트는 `surface` 가 아니라 **`surfaceContainer` 계열**을 쓴다.
  그래서 `surface` 만 브랜드 색으로 바꾸면 **평평한 배경만 바뀌고 떠 있는 것들은 보라로 남는다.**
- → `surfaceContainerLowest`~`Highest` · `surfaceVariant` · `outline` · `outlineVariant` ·
  `inverseSurface` · `tertiary` 를 웜 뉴트럴 램프로 직접 지정했다 (`Color.kt`).
- 라이트·다크 양쪽에서 확인했다. 다크는 딥그린 대신 `BrandGreenDark`(`#8FD3AC`)가 포커스에 뜬다.

⚠️ **색을 추가할 때는 스크린샷으로 확인한다.** 이 버그는 컴파일도 되고 테스트도 통과했다.
바텀시트(S-02)·카드(장소 목록)를 붙일 때 같은 종류가 또 나올 수 있다.

---

### 29. 첫 실행 검증과 그때 드러난 것들 (2026-09-05)

에뮬레이터(AVD `lotto_test`, android-33)에 처음 올려 전 경로를 눌러 봤다.
**시도 16개에 「전남광주통합특별시」가 실제로 뜨는 것**(D-29)까지 화면으로 확인했다.
3단 연동 · 최근 지역이 앱 재시작 후에도 유지 · 라이트/다크 모두 정상.

**D-46. Supabase 요청 타임아웃을 8초로 명시한다.**

네트워크를 끊고 앱을 켰더니 **10초가 지나도 스켈레톤**이었고 30초 시점에야 실패로 넘어갔다.

- 에러 처리 자체는 맞았다 — `runCatchingList` 가 `UiState.Failed` 로 바꾸고 화면에
  "정보를 불러오지 못했습니다" + 재시도 버튼이 나온다. **문제는 그게 너무 늦게 나오는 것이었다.**
- `spec.md §5.3` 은 실패를 재시도 버튼과 함께 **보여줄 것**을 요구한다.
  10초 넘게 스켈레톤이면 사용자는 앱이 멈춘 줄 안다. 늦게 보여주는 것은 지킨 것이 아니다.
- → `SupabaseProvider` 에 `requestTimeout = 8.seconds`. 재측정하니 11초 시점에 실패 상태였다.
- 지하철·엘리베이터에서 그대로 재현되는 상황이다. 실기기에서도 확인할 것.

**[알려진 빈틈] 오프라인으로 시작하면 최근 지역이 돌아오지 않는다.**

`observeRecent` 는 `recentStore.codes` 플로우를 한 번 받아 조회하고, 실패하면 빈 목록을
넣는다(설계상 최근 지역은 부가 기능이라 화면 전체를 실패로 만들지 않는다). 그래서
**네트워크가 복구돼도 칩이 돌아오지 않는다** — 앱을 다시 켜야 한다.
`spec.md §5.3` 의 오프라인 캐시 요구사항과 함께 처리하는 것이 맞다. 지금은 남겨 둔다.

**[status 표시 정정] 중심좌표의 모수는 읍면동이다.**

`run.py status` 가 중심좌표를 `5,067 / 5,339` 로 보여 "진행중" 으로 찍었다. 좌표는
읍면동에만 채우므로 시도·시군구 272행은 애초에 대상이 아니다. **모수를 잘못 잡으면
다 채워도 영원히 완료가 안 된다.** → `COLUMNS` 에 모수 level 을 붙여 `5,067 / 5,067 완료` 로 고쳤다.

---

### 30. 시안 기준 디자인 적용 (2026-09-05)

**D-47. 시안의 시각 언어를 S-01 에 적용한다. 정보구조는 D-39 를 유지한다.**

D-39 에서 "시안의 비주얼은 받고 정보구조는 `spec.md` 를 지킨다"고 정했고, 그 비주얼을
실제로 구현한 것이다. 시안에서 가져온 것:

- **배경과 카드를 분리했다.** 시안의 바탕은 순백이 아니라 **웜 크림**(`#FAF7F0`)이고
  그 위에 **흰 카드**가 뜬다. 전에는 `surface` 하나로 둘 다 처리해 평평했다.
  이 대비가 시안 톤의 핵심이다.
- **곡률을 올렸다.** 카드 20dp, 검색창·주요 버튼·칩은 **알약**. M3 기본값보다 눈에 띄게 둥글다.
  → `ui/theme/Shape.kt` 신설. **값을 바꾸면 화면 인상이 통째로 바뀌므로 여기서만 고친다.**
- **알약 검색창 + 돋보기 아이콘.** 시안에서 가장 눈에 띄는 요소다.
- **3단 드롭다운을 흰 카드 하나로 묶었다.** 시안이 관련 항목을 카드로 묶는 방식이다.
- **헤드라인을 딥그린으로.** 시안의 "오늘은 어디로 함께 갈까요?" 자리에
  같은 역할의 문구를 놓았다.
- **상단에 선택 지역을 읍면동까지 항상 표시.** D-39 의 요구를 이 화면에서 먼저 구현했다.

시안에서 **가져오지 않은 것**은 D-40 그대로다 — 별점·후기·장소 사진·큐레이션.
캐릭터 일러스트도 넣지 않았다. `spec.md §6.1`("차분한 안내자")과 어긋나고,
일러스트는 APK 크기 예산(30MB)에도 영향을 준다.

**[부수 수정] 최근 지역 칩에 시군구를 함께 표시한다.**
전에는 `shortName` 만 써서 "신사동"처럼 **여러 구에 같은 이름이 있는 동**이 구분되지 않았다.
→ "강남구 대치동" 형태로 바꿨다. 최근 지역은 3개까지 나란히 놓이므로 구분이 필수다.

라이트·다크 양쪽을 에뮬레이터 스크린샷으로 확인했다 (D-45 의 교훈 — 색과 형태는 화면으로만 검증된다).

---

### 31. 0단계 완료 · 동물병원 API 스키마 확보 (2026-09-05)

**D-48. `localdata_cd` 는 인증키 없이 문서만으로 채운다.**

LOCALDATA 인증키를 기다리느라 0단계가 열려 있었는데, **지역코드는 API 가 아니라
문서로 배포된다**는 것을 알고 나서 바로 끝났다. `etl/docs/` 의 개방자치단체코드
엑셀을 읽어 `regions` 에 채운다 — **미매핑 0 · 5,338/5,339**.

- 비는 1행은 `전남광주통합특별시` **시도** 행이다. LOCALDATA 코드가 광주+전남 둘로
  갈려 한 칸에 못 담는다. `mapping.py` 가 TourAPI 에서 내린 것과 같은 판단이며,
  시군구 27개는 전부 정상 매핑됐으므로 실사용에 지장이 없다.
- ⚠️ **LOCALDATA 도 옛 체계다** (2026-09-05 실측). `전남광주통합특별시` 가 없고
  인천 신설 자치구(제물포·영종·서해·검단)도 없다. → D-29·D-30 예외표를 그대로 옮겼다.
  **이로써 세 번째 사례다** — APMS 만 개편을 반영했고 TourAPI·LOCALDATA 는 아직이다.

**이름 규칙** — 시군구는 `짧은시도명 + 시군구명`(`서울강남구`), 일반구는 없어서 모시로 접는다
(`경기성남시`). 시도 행은 `_ALL`(`6110000_ALL`)을 쓴다 — '그 시도 전체'라는 뜻이라
시도 행의 의미와 맞고, **접두어 없는 시도명이 아예 없는 시도가 있다**(강원특별자치도).

**D-49. 동물병원 API 는 LOCALDATA 계열이다. 스키마를 확보했고 호출 경로만 남았다.**

미리보기 응답으로 필드를 전부 확인했다 (`etl/docs/15154952-동물병원-응답스키마.md`).
`totalCount` 10,617. 여기서 드러난 것 셋:

1. **좌표가 경위도가 아니다.** `CRD_INFO_X=187381.28` 형태의 평면직각좌표(EPSG:5174 계열)다.
   `pyproj` 변환이 필요하고, **변환 후 실제 좌표로 검산해야 한다.** 빈 값인 행도 있다 —
   버리지 말고 주소를 살려 나중에 지오코딩한다.
2. **읍면동은 코드가 아니라 주소로 배정한다.** `OPN_ATMY_GRP_CD` 는 시군구까지만 가리킨다.
   D-31 과 같은 판단이다. ⚠️ **주소는 새 체계로 온다** (`전북특별자치도`, `화성시 동탄구`).
   개방자치단체코드 문서는 옛 체계다. **둘을 섞으면 안 된다.**
3. **폐업을 걸러야 한다.** `SALS_STTS_CD` `01` 만 영업이다. 코드표는 같은 엑셀 2번 시트에 있다.
   `plan.md` 1단계 완료 기준이 "폐업 업소가 표시되지 않는다"이다.

**아직 막힌 것** — 서비스 경로(`.../1741000/animal_hospitals`)는 알지만 **오퍼레이션명을 모른다.**
대조 실험으로 원인을 좁혔다: 같은 호스트의 `StanReginCd/getStanReginCdList` 는
`403 SERVICE_KEY_IS_NOT_REGISTERED`(경로는 있음), `animal_hospitals` 계열 8종은 전부
`400 NO_OPENAPI_SERVICE`(경로 없음). **호스트·키·요청 모양은 정상이고 이름만 모른다.**
→ 개발계정 상세 > 상세기능정보 > [미리보기] 화면 **상단의 요청 URL** 이 있으면 즉시 풀린다.

---

### 32. 1단계 동물병원 ETL (2026-09-05)

엔드포인트는 `.../animal_hospitals/**info**` 였고 파라미터는 `_type` 이 아니라 **`returnType`**,
`resultCode` 는 **`"0"`** 한 자리다(APMS `"00"` · TourAPI `"0000"` 과 또 다르다 → `OK_CODES` 확장).
D-37 이 여기서 닫힌다. **오퍼레이션명은 끝까지 추측으로 못 맞췄다** — 8종을 시도해 전부 틀렸다.

**D-50. 좌표계는 EPSG:5174 다. 추측하지 않고 지오코딩으로 검산했다.**

`CRD_INFO_X=187381.28` 은 경위도가 아니다. 후보 4종을 카카오 지오코딩 결과와 대조했다.

| 좌표계 | 실주소 대비 오차 |
|---|---|
| **EPSG:5174** | **1m 이내** ✅ |
| EPSG:2097 | 321m |
| EPSG:5186 | 약 100km |
| EPSG:5179 | 전혀 다른 위치 |

⚠️ **2097 이 가장 위험했다.** 겉보기엔 맞는데 **일관되게 321m** 어긋난다. 지도에 찍으면
"옆 건물"로 보여 눈으로는 절대 못 잡는다. → 기준점 2개를 단위 테스트로 고정했다.

⚠️ **`(0,0)` 은 EPSG:5174 에서 제주 남서 해상(33.48, 124.85)으로 변환돼 한반도 범위 검사를
그냥 통과한다.** 테스트가 이걸 잡았다. 실제 평면좌표는 10만 단위이므로 1000 미만은
값 없음으로 본다(`MIN_PLANE_COORD`).

**D-51. 읍면동은 주소 문자열로 배정하고, 못 맞춘 것만 카카오로 보완한다.**

`OPN_ATMY_GRP_CD` 는 시군구까지만 가리킨다(D-31 과 같은 판단). 주소 앞부분을
`regions.full_name` 과 맞추는데 **토큰 수가 지역마다 다르다** — 4개(일반구가 있는 시)·
3개(보통)·2개(세종은 시군구가 없다). 4→3→2 순으로 시도한다.

⚠️ **D-29 예외가 방향이 반대로 필요했다.** `regions` 는 `전남광주통합특별시` 인데
인허가 주소는 `광주광역시`·`전라남도` 로 온다. `mapping.py` 의 예외표(우리 이름→외부 이름)와
반대 방향이라 표를 따로 뒀다.

**[중요] 인허가 주소에는 폐지된 법정동이 남아 있다.**
`경기도 화성시 동탄구 오산동` 이 안 맞아 파고들었더니 법정동코드 파일에서 **「폐지」**
였다(화성시 39개 동이 파일·DB 모두 일치 — **우리 ETL 은 정상**). 영업 데이터만 옛 동명을
쓰고 있는 것이다. 카카오가 `b_code` 로 **오산동 → 여울동(현행)** 을 되돌려 준다.
→ 이름 매칭 실패분만 카카오에 물어 **법정동코드와 좌표를 한 번에** 받는다.
지번이 안 되면 도로명으로 한 번 더 시도한다(인천 중구→제물포구처럼 지번 전체가 옛 체계라
카카오도 못 읽는 경우가 있다).

**적재 후 DB 실측 (2026-09-05)** — `places` 10,617행, 전부 `hospital`.

| | 건수 | 비고 |
|---|---|---|
| `status=open` | **5,474** | 전국 동물병원 실제 규모와 맞는다 |
| `status=closed` | 5,115 | LOCALDATA 는 이력을 통째로 준다 |
| `status=suspended` | 28 | 휴업 |
| 영업중 · 좌표 있음 | **5,470** (99.9%) | `geom` 도 같은 수만큼 자동 생성됐다 |
| 영업중 · 지역 배정됨 | **5,447** | |
| 영업중 · **지역 미배정** | **27** (0.5%) | 아래 '알려진 한계' |

**D-52. 모르는 영업상태코드는 `closed` 로 본다.**
`open` 을 기본값으로 두면 폐업이 지도에 남는다. `plan.md` 1단계 완료 기준이
"폐업 업소가 표시되지 않는다"이다. 전체 10,617건 중 **약 5,143건이 폐업·휴업**이다 —
LOCALDATA 는 이력을 통째로 주므로 이 비율이 정상이다.

**[알려진 한계] `region_code` 가 NULL 인 행이 남는다 — 영업중 기준 27건(0.5%).**
주소가 아예 빈 행이 193건 있고(그중 174건이 폐업), 개편 이전 주소 일부는 카카오도 못 읽는다
(인천 중구→제물포구처럼 지번 전체가 옛 체계인 경우).
이 행들은 **지역 필터에는 안 잡히지만 좌표가 있으면 `places_nearby`(반경 검색)에는 잡힌다.**
시군구 코드로 대충 채우지 않는다 — 읍면동 필터가 조용히 틀린 답을 내게 된다.
`영업중인데 지역 미배정` 을 ETL 로그에 따로 찍어 이 수치를 계속 감시한다.

---

### 33. S-00 홈 + 하단 탭 4개 (2026-09-05)

D-39 가 설계한 정보구조를 실제로 만들었다. 화면 코드를 쓰다가 **문서만 봐서는
안 보이던 것 넷**이 나왔고, 넷 다 "사용자가 잘못 읽을 여지"에 관한 것이다.

**D-53. 적재 안 된 카테고리는 `0곳` 이 아니라 `준비 중` 으로 적는다.**

홈 그리드 6칸 중 지금 데이터가 있는 것은 동물병원 하나뿐이다. 나머지 다섯을 그냥 세면
전국 어느 동네에서나 `0곳` 이 나오고, 사용자는 그것을 **"이 동네엔 미용실이 없구나"** 로 읽는다.
`spec.md §5.3` 이 "데이터 없음"과 "불러오기 실패"를 절대 같은 화면으로 처리하지 말라고 한 것과
같은 이유로, **"이 동네에 없다"와 "우리가 아직 안 실었다"도 갈라야 한다.**

- → `PlaceCategory.loaded` 플래그. `false` 인 카테고리는 **질의 자체를 하지 않고** `준비 중` 을 적는다.
- 단계별 ETL 이 끝날 때마다 **한 줄만 `true` 로** 바꾼다. `plan.md` 2단계 완료 기준
  ("앱 코드 수정이 필터 칩 1줄 추가 수준")과 같은 크기의 변경이다.
- 적재된 카테고리의 `0곳` 은 그대로 둔다. 그건 **유효한 답**이다.

**D-54. 현재 지역은 따로 저장하지 않는다. 최근 목록의 맨 앞이 현재 지역이다.**

홈 지역 칩과 S-01 이 같은 값을 봐야 한다. 현재 지역용 키를 따로 두면 두 값이 어긋날 수 있고,
어긋나면 "홈은 대치동인데 S-01 은 연남동" 같은 상태가 된다.
`RecentRegionStore.remember()` 가 이미 최근 목록 맨 앞에 올리므로, **맨 앞을 현재 지역으로 읽으면
동기화 문제가 애초에 생기지 않는다.** 대신 S-01 의 최근 지역 칩도 반드시
`remember()` 를 거치게 했다 — 그러지 않으면 칩으로 고른 지역이 홈에 반영되지 않는다.

**D-55. 홈 그리드에서 지도로 갈 때도 평범한 push 가 아니라 탭 전환으로 간다.**

⚠️ **실측 버그였다.** 홈 타일 → `navigate("map")` 로 지도를 홈 **위에** 얹었더니,
지도가 홈 탭의 저장된 백스택에 딸려 들어가서 **나중에 홈 탭을 눌렀을 때 홈 대신 지도가 떴다.**
하단 탭은 `popUpTo(start){saveState}` + `restoreState` 로 각 탭 상태를 보존하는데,
그 보존 대상에 "홈 위에 얹힌 것"이 전부 포함되기 때문이다.

- → `NavHostController.switchTab()` 하나로 모았다. **하단 탭과 홈 그리드가 같은 방식으로 움직인다.**
- 판단 기준: **탭인 화면은 다른 탭 위에 얹지 않는다.** S-01 지역 선택처럼 탭이 아닌 화면만
  홈 위에 push 한다(그때는 탭바를 숨긴다).
- 화면 넷 중 셋이 아직 껍데기인 지금도 이 버그가 났다. 지도·상세가 붙으면 훨씬 찾기 어려웠을 것이다.

**D-56. 카테고리 5색에 다크 짝을 둔다.**

`spec.md §6.2` 의 카테고리 5색은 **라이트 배경 기준**이다. 어두운 카드 위에 그대로 올렸더니
딥그린 건수(`#2E6B4F`)가 거의 안 보였다 — §6.5 의 명도 대비 4.5:1 을 못 지킨다.
브랜드 색이 `BrandGreen` / `BrandGreenDark` 짝을 갖는 것과 같은 이유로 5색에도 짝을 만들었다.
D-45 의 교훈이 그대로 반복됐다 — **색은 화면으로만 검증된다.**

**[부수] 실패했을 때 지역 칩 문구를 "지역을 골라 주세요"와 갈랐다.**
저장된 지역은 있는데 이름을 못 읽은 것이므로 "골라 주세요"로 적으면 사용자가 **자기 선택이
날아간 줄 안다.** → `지역을 불러오지 못했습니다`. 첫 실행 안내 문구도 이때는 감춘다.

**에뮬레이터 실측 (2026-09-05)** — 라이트/다크 · 탭 4개 왕복 · 홈 타일→지도 ·
S-01 왕복(드롭다운 3단 + 최근 칩) · 오프라인 실패와 재시도 회복까지 확인했다.
건수는 PostgREST 직접 조회와 대조했다 — **삼성동 8 · 대치동 7 로 일치.**

---

### 34. 장소 목록 · S-03 상세 (2026-09-05)

홈에서 `동물병원 8곳` 을 눌러도 **그 8곳을 볼 화면이 없었다.** 그 길을 이었다.

**D-57. 길찾기·전화·공유는 지도 키 없이 지금 된다. 대기 목록에서 뺀다.**

`plan.md` 와 `README` 가 "길찾기는 카카오 네이티브 앱 키 대기"로 적어 두었는데 **틀렸다.**
네이티브 앱 키는 **지도를 앱 안에 그릴 때**(S-02) 필요한 것이고, 길찾기는
카카오맵 공개 링크(`map.kakao.com/link/to/이름,위도,경도`)로 넘기면 된다.

- 에뮬레이터에서 실측했다 — 카카오맵 앱이 없으면 웹이 받아 "설치없이 지도보기"를 준다.
- 전화는 `ACTION_DIAL` 이다. **권한이 필요 없고**, 번호를 채워 주기만 하고 발신은 사용자가 누른다.
  `ACTION_CALL` 을 쓰면 `CALL_PHONE` 권한이 필요해지는데, 그건 이 앱이 질 이유가 없는 부담이다.
- **받아 줄 앱이 없으면 안내한다.** 조용히 아무 일도 안 일어나면 사용자는 앱이 고장난 줄 안다.

→ 결과적으로 **S-03 의 요구사항이 지도 키 없이 전부 채워졌다.** 남은 것은 S-02 지도뿐이다.

**D-58. 기준일에 시간대 변환을 하지 않는다.**

⚠️ 원본(`DAT_UPDT_PNT`)은 **시간대가 없는 값**인데 ETL 이 UTC 로 못박아 저장했다
(`2026-03-20T22:02:03+00:00`). 화면에서 KST 로 바꾸면 22:02 가 넘어가 **기준일이 하루 밀린다.**
→ 문자열에서 **날짜 부분만 그대로 떼어** 쓴다. 단위 테스트로 고정해 두었다.

기준일이 없는 행도 있다. 그때는 `기준일 없음` 이라고 적는다 — **지어내지 않는다.**
출처·기준일 표기는 의무다(`spec.md §5.2`·`§8`).

**D-59. 목록은 거리순이 아니라 이름순이다.**

거리순이 더 자연스러워 보이지만 그건 **현재 위치**가 있어야 하고, 이 앱의 기준점은
현재 위치가 아니라 **내가 고른 지역**이다(D-24·D-39). 읍면동 하나 안에서는 거리 차이가
크지 않아 정렬 기준으로서 값어치도 적다. 거리 표기는 위치 권한이 붙는 시점에 카드에 더한다.

**[부수] 카테고리 표기를 `ui/common/CategoryUi.kt` 한 곳으로 모았다.**
홈 그리드·목록 배지·상세 머리말이 같은 표를 읽는다. 표가 두 벌이 되면 카테고리를 늘릴 때
한쪽만 고치게 되고, 그게 `plan.md` 2단계 완료 기준("필터 칩 1줄 추가")을 깨뜨린다.

**[부수] 목록 상한을 300으로 우리가 정했다.**
PostgREST 기본 상한(1000)에 **조용히 잘리는** 것보다, 우리가 정한 값에서 끊는 편이 낫다.
읍면동 하나의 한 카테고리가 300을 넘을 일은 없다(전국 최다도 수십 곳이다).

**에뮬레이터 실측 (2026-09-05)**

| 확인한 것 | 결과 |
|---|---|
| 삼성동 동물병원 목록 | **총 8곳** — 홈 건수와 일치 |
| 상세 | 이름·주소 2종·전화·영업상태 + **출처 지방행정인허가데이터 · 기준일 2026-01-14** |
| 전화 | 다이얼러가 `025417515` 를 채워서 열린다 |
| 길찾기 | 카카오맵 링크가 열린다 (**키 없이**) |
| 빈 목록 | 강릉시 강동면 → 홈 `0곳`, 목록 `이 지역에는 등록된 동물병원 정보가 없습니다` |
| `0곳` vs `준비 중` | 같은 화면에서 갈린다 (동물병원 0곳 · 미용 준비 중) — D-53 이 노린 그림이다 |

---

### 35. S-07 즐겨찾기 (2026-09-05)

**D-60. 즐겨찾기는 id 만 담지 않는다. 이름·주소를 함께 베껴 둔다.**

즐겨찾기는 **"이따 다시 볼 곳"** 이라 지하철·산속처럼 網이 없는 데서 열릴 확률이 높다.
id 만 저장하면 그때 서버에 물어야 하고, 못 물으면 **빈 목록**이 뜬다. 담아 둔 게 안 보이는
즐겨찾기는 즐겨찾기가 아니다.

- → `favorite_places` 에 `name`·`address`·`tel`·`category` 를 함께 넣는다.
- 원본이 바뀌면 값이 낡을 수 있다. 그래도 **낡은 이름이 빈 화면보다 낫고**, 상세를 열면
  최신으로 덮인다(같은 id 를 다시 담으면 `REPLACE` 다).
- **에뮬레이터에서 앱을 죽이고 網까지 끊은 뒤 확인했다** — 이름·주소·전화가 그대로 떴다.

**D-61. Room 을 쓴다. KSP 는 Kotlin 과 짝이 맞는 것이 없어 독립 버전 라인을 쓴다.**

`plan.md` 가 Room 을 적어 두었고, DataStore(문자열 저장)로 대신할까 검토했으나 Room 으로 갔다.
반응형 조회(`Flow`)를 DAO 가 그냥 준다는 점이 컸다 — 상세 화면의 북마크가 즐겨찾기 목록과
저절로 맞물린다.

⚠️ **KSP 버전 함정.** `<코틀린>-<KSP>` 형식(`2.2.21-2.0.5`)은 **Kotlin 2.3.21 용이 없다.**
이 프로젝트는 AGP 8.13.2 / Kotlin 2.3.21 에 고정돼 있다(D-36). 대신 KSP 가 코틀린 버전과
분리된 새 라인(`2.3.11`)을 내놓았고, 그쪽이 Kotlin 2.3.21 에서 동작한다 — 빌드로 확인했다.
→ `libs.versions.toml` 의 `ksp = "2.3.11"`. **버전을 올릴 때 이 라인인지 먼저 확인할 것.**

**[부수] `exportSchema = true` 로 둔다.**
스키마 JSON(`app/schemas/`)이 있어야 나중에 마이그레이션을 쓸 수 있다. 끄면 버전을 올리는
순간 "이전 스키마를 모른다"가 되고, 그때는 사용자 데이터를 지우는 수밖에 없다.

**[알려진 한계] 오프라인에서 즐겨찾기를 눌러 상세를 열면 실패 화면이 뜬다.**
목록은 스냅샷으로 뜨지만 상세는 서버를 다시 부른다. 스냅샷에는 **출처·기준일이 없어서**
그대로 상세를 그리면 의무 표기(`spec.md §5.2`)를 어긴다 — 그래서 대충 메우지 않았다.
`§5.3` 의 오프라인 캐시를 할 때 `source`·`source_updated_at` 을 스냅샷에 더해 함께 푼다.

---

### 36. 오프라인 캐시 (2026-09-06)

`spec.md §5.3` 이 요구하는 마지막 줄 — **"오프라인이면 마지막 조회 결과를 캐시에서 표시 +
상단에 오프라인 배너"** — 을 채웠다. D-61 이 남긴 한계(오프라인 즐겨찾기 상세)도 여기서 닫았다.

**D-62. 캐시의 정의는 "마지막 조회 결과"다. 그리고 빈 사본은 캐시가 아니라 미스다.**

목록을 새로 받으면 그 읍면동·카테고리의 사본을 **통째로 갈아 끼운다**(`replacePlacesIn`).
덮어쓰기만 하면 폐업으로 목록에서 빠진 곳이 사본에 남아 **오프라인에서만 계속 보인다.**

더 중요한 쪽은 두 번째다. 사본이 비어 있으면 성공이 아니라 **실패로 넘긴다.**
빈 값을 돌려주면 화면이 "이 지역에는 등록된 동물병원 정보가 없습니다"를 그리는데,
우리는 없다는 것을 안 게 아니라 **모르는 것**이다. `§5.3` 이 "데이터 없음"과 "불러오기 실패"를
절대 합치지 말라고 한 그 자리이며, D-53(`0곳` vs `준비 중`)과 같은 규칙의 세 번째 적용이다.

- → 건수만은 예외적으로 표(`cached_counts`)를 따로 둔다. 목록 사본이 비었다고 `0곳`으로
  적으면 안 되지만, **서버가 실제로 준 0** 은 유효한 답이라 그 둘을 구분해 담아야 한다.
- → 규칙 자체는 `data/OfflineCache.kt` 의 `fetchOrCached()` 한 곳에 있다. 저장소 두 개가
  같은 순서(서버 → 성공하면 사본 남기기 → 실패하면 사본 꺼내기)를 지키게 하기 위해서다.

⚠️ **`CancellationException` 을 먼저 잡아 다시 던진다.** 코루틴 취소도 예외로 오기 때문에
`Throwable` 만 잡으면 화면을 떠난 뒤에도 캐시를 뒤지고 결과를 흘려보낸다.

**D-63. 지역을 장소보다 먼저 캐시한다. 그리고 지역 사본은 지우지 않는다.**

지역 이름이 없으면 오프라인에서 **홈의 지역 칩부터 비어** 아무 화면도 그릴 수 없다.
전국을 다 훑어도 5,339행(시도 16 · 시군구 256 · 읍면동 5,067)이고 컬럼도 짧아서,
오래된 것을 밀어내는 규칙을 두면 하필 시도 16개가 밀려 나가 3단 드롭다운의 첫 칸이 비는
쪽이 더 나쁘다. 장소 사본만 2,000행에서 끊는다(`MAX_CACHED_PLACES`).

**전국 10,617건을 통째로 내려받지 않는다.** 그건 캐시가 아니라 사본이고, 기준일이 낡는
순간 의무 표기(`§5.2`)가 거짓이 된다.

**D-64. 즐겨찾기 스냅샷을 완성해 D-61 의 한계를 닫는다. 옛 스냅샷은 그리지 않는다.**

`favorite_places` 에 `source`·`status`·`address_road`·`address_jibun`·`lat`·`lng`·`extra`·
`source_updated_at` 을 더했다. 이제 담아 둔 곳은 **캐시가 밀려 나간 뒤에도** 상세가 열린다 —
캐시는 지워도 되는 사본이지만 즐겨찾기는 사용자가 만든 것이라 지우지 않기 때문이다.
그래서 즐겨찾기가 캐시를 참조하지 않고 자기 사본을 갖는다(D-60 의 연장).

⚠️ **버전 1 때 담은 즐겨찾기는 상세를 그리지 않는다.** 출처가 없기 때문이다. 목록에는 그대로
뜨지만 상세는 종전대로 실패 화면이다. 출처를 "공공데이터" 같은 말로 메우면 화면은 채워지지만
그건 **없는 사실을 적는 것**이고, 이 앱이 신뢰를 얻는 유일한 근거를 깎는다.

**D-65. 마이그레이션 SQL 은 목록으로 빼서 스키마 JSON 과 기계로 대조한다.**

⚠️ 이 트랩은 눈으로 못 잡는다. 손으로 쓴 `CREATE TABLE` 이 Room 이 만드는 문장과 **한 글자만
달라도** 앱은 빌드되고 테스트도 통과한 뒤, **버전 1 을 깔아 둔 기기에서만**
`IllegalStateException: Migration didn't properly handle` 로 죽는다. 새로 깐 기기는 표를
새로 만들기 때문에 개발 중에는 절대 드러나지 않는다.

- → 문장을 `MIGRATION_1_2_SQL` 목록으로 빼고, `MigrationSqlTest` 가 `exportSchema` 로 남긴
  `schemas/.../2.json` 과 대조한다. 표·인덱스·새 컬럼이 하나라도 빠지면 테스트가 깨진다.
- → **`fallbackToDestructiveMigration()` 을 쓰지 않는다.** 즐겨찾기는 사용자가 직접 담은
  것이고 서버에 사본이 없다(로그인이 없다). 버전을 올렸다고 날리면 복구할 방법이 없다.
- → 새 컬럼은 **선언 순서도 맨 뒤**로 맞춘다. `ALTER TABLE ADD COLUMN` 이 열을 뒤에 붙이므로
  그래야 새로 깐 기기와 올려 깐 기기의 표가 같아진다.

**D-66. 오프라인 배너는 오류가 아니다. 그리고 받아 둔 시각을 함께 적는다.**

⚠️ **오류 빨강(`error`)을 쓰지 않는다.** 그 색은 응급·오류 전용이다(`§6.2`). 오프라인은
오류가 아니라 상태다. `secondaryContainer` 도 쓰지 않는다 — 테마가 그 칸을 채워 두지 않아
**M3 기본 보라**가 나온다(D-45 와 같은 함정). 팔레트에 실제로 있는 `surfaceContainerHigh` 를 쓴다.

시각을 적는 이유는 D-40 의 연장이다. 이 앱은 상세마다 기준일을 적어서 신뢰를 얻는 도구인데,
사본으로 그린 화면에만 시각이 없으면 그 약속이 화면마다 어긋난다. 상세에서는
**받아 둔 시각(배너)** 과 **원본 기준일(하단)** 이 나란히 보이며, 둘은 다른 값이다.

**[부수] S-01 의 최근 지역 빈틈을 함께 고쳤다.**
`다시 시도` 가 시도 목록만 다시 부르고 있었다. 최근 지역 칩은 `recentStore.codes` 가 바뀔 때만
다시 그려지는데 코드는 그대로라 흐름이 다시 흐르지 않아, **網이 살아나도 칩이 안 돌아왔다.**
→ `retry()` 가 시도 목록과 최근 지역을 함께 다시 부른다.

**에뮬레이터 실측 (2026-09-06)** — 버전 1 DB 가 깔린 기기 위에 덮어 설치해서 확인했다.

| 확인한 것 | 결과 |
|---|---|
| 1 → 2 마이그레이션 | `user_version=2` · 캐시 표 3개 생성 · **버전 1 즐겨찾기 한 줄 그대로 남음** |
| 온라인 홈·목록·상세 | 종전과 같음. **배너 없음** (지금 받은 값이므로) |
| 비행기모드 홈 | 배너 + `서울특별시 강남구 삼성동` + `8곳` (사본) |
| 비행기모드 목록 | 배너 + 총 8곳, 카드 그대로 |
| 비행기모드 상세 | 배너(`받아 둔 시각`) + 하단 `출처 · 기준일 2026-03-20` **둘 다** |
| 캐시가 밀려 나간 즐겨찾기 | `cached_places` 행을 지우고 열었더니 **스냅샷으로 열렸다** (D-64) |
| 비행기모드 S-01 | **최근 지역 칩이 뜬다** · 시도 목록은 사본이 없어 실패 화면 (빈 목록 아님 — D-62) |
| 網 복구 후 `다시 시도` | 시도 목록 복구 · **최근 지역 3개 전부 복구** · 배너 사라짐 |
| 다크 모드 배너 | 대비 정상, M3 기본 보라 없음 (D-66) |

**[알려진 한계] 오프라인에서 사본이 뜨기까지 8~16초 걸린다.**
서버 요청이 타임아웃(8초)을 다 쓰고 **실패해야** 캐시로 넘어가는 구조다. 홈은 지역 8초 +
건수 8초라 최악 16초다. 실제 기기에서 圈外면 DNS 가 즉시 실패해 훨씬 빠르지만, 그건 운이다.
제대로 고치려면 **사본을 먼저 그리고 뒤에서 갱신**하는 구조(cache-first)로 바꿔야 하고,
저장소가 값을 두 번 흘려보내야 하므로 `suspend` 를 `Flow` 로 바꾸는 작업이 따라온다.
⚠️ 그때도 **사본을 그리는 동안 배너를 띄우면 안 된다** — 網이 멀쩡한데 "오프라인"이 잠깐
보이는 것은 D-62 가 막으려는 것과 같은 종류의 거짓말이다. 요청이 실패한 뒤에 띄워야 한다.
