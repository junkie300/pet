# 0단계 최소 의존성
supabase==2.15.1        # Supabase Python 클라이언트 (PostgREST 래퍼)
python-dotenv==1.0.1    # .env 로딩
requests==2.32.3        # 공공 API 호출
openpyxl==3.1.5         # LOCALDATA 개방자치단체코드 엑셀 파싱 (localdata 명령에서만)
pyproj==3.7.2           # 동물병원 좌표 EPSG:5174 → 4326 (hospitals 명령에서만)

# 이후 단계에서 필요해지면 주석을 푼다
# pandas==2.2.2         # 4단계: 식약처 Excel 파싱
