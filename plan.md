# plan.md — 실행 계획

> 문서 역할: **어떻게, 어떤 순서로, 언제 만드는가**
> 무엇을 만드는지는 `spec.md`, 왜 만드는지는 `개발계획서.md`
> 버전 0.1 · 2026-08-29

---

## 0. 이 계획의 원칙

1. **재사용도 높은 순서로 붙인다.** 앞 단계의 결과물을 그대로 쓰는 기능을 먼저 배치한다.
2. **2·3단계는 설계 검증 장치다.** 여기서 코드가 재사용되지 않으면 1단계 설계로 되돌아간다.
3. **출시 준비는 개발과 병행한다.** 테스터 모집은 개발이 끝난 뒤 시작하면 늦는다.
4. **MVP를 먼저 테스트 트랙에 올린다.** 대기 시간을 개발 시간과 겹친다.

---

## 1. 단계 개요

| 단계 | 목표 | 산출물 | 예상 |
|---|---|---|---|
| **0** | 지역코드 정규화 | `regions` 테이블 완성 | 2주 |
| **L** | 학습 (0~1단계와 병행) | Kotlin/Compose 기초 | 4주 |
| **1** | 동물병원 — **앱의 뼈대 전체** | 지도·검색·목록·상세 동작 | 6주 |
| **2** | 미용시설 — *재사용 검증* | 업종코드 변경만으로 추가 | 1주 |
| **3** | 야생동물구조센터 — *재사용 검증* | 소스 교체만으로 추가 | 1주 |
| **★** | **MVP 비공개 테스트 시작** | 테스터 12명 배포 | — |
| 4 | 동반 식당 | 식약처 ETL + 안내 문구 | 2주 |
| 5 | 관광정보 | TourAPI 연동 | 2주 |
| 6 | 보호센터·입양 | [입양] 탭 신규 | 4주 |
| 7 | 출시 준비 | 스토어 등록·심사 | 2주 |

**총 예상 약 6개월** (주 10~15시간 투입, 앱 개발 학습 곡선 포함 기준)

---

## 2. 단계별 상세

### 0단계 — 지역코드 정규화 테이블 ★ 최우선

가장 중요하면서 가장 눈에 띄지 않는 작업. **이후 모든 단계가 여기에 의존한다.**

- [x] Supabase 프로젝트 생성, PostGIS 확장 활성화
- [x] `regions` 테이블 생성 (`spec.md` §3.1)
- [x] 기준 데이터 적재 (시도 16 · 시군구 256 · 읍면동 5,067)
      ※ TourAPI 법정동코드정보가 아니라 **행정표준코드관리시스템 전체자료**를 썼다 (D-31)
- [x] 개방자치단체코드 매핑 (`localdata_cd`) — 인증키가 아니라 **문서로** 끝났다.
      빈 곳은 `전남광주통합특별시` 한 행뿐이고 그건 코드가 둘로 갈려 비우기로 한 자리다 (D-48)
- [x] 국가동물보호정보시스템 시도·시군구 코드 매핑 (`apms_upr_cd`, `apms_org_cd`)
- [x] TourAPI areaCode·sigunguCode 매핑 (`tour_area_cd`, `tour_sigungu_cd`)
- [ ] 읍면동 중심좌표 채우기 ← **카카오 REST API 키 필요** (ETL 은 작성 완료, 키만 있으면 실행)
- [x] RLS 정책: `anon` SELECT만 허용

**완료 기준 (DoD)**
- [x] 전국 읍면동이 빠짐없이 들어있다 (5,067)
- [x] 임의의 읍면동 10곳을 골라 4개 외부 코드가 모두 정확히 매핑된다
      → APMS·TourAPI 4개 컬럼은 **미매핑 0건**으로 확인 (2026-09-02).
        `localdata_cd` 도 5,339행 중 1행(통합시도)만 비어 있고 그건 의도한 자리다 (D-48·D-93)
- [x] `parent_code` 계층이 끊긴 곳이 없다

> ⚠️ 이 단계에서 **행정동/법정동 불일치**를 만나게 된다. 기준을 법정동으로 고정하고 문서에 남길 것.

---

### L단계 — 학습 (0·1단계와 병행)

- [ ] Kotlin 기초 문법
- [ ] Compose 기본 (State, Composable, 목록)
- [ ] Android Studio · Gradle · 에뮬레이터
- [ ] "목록 → 상세" 앱 하나를 처음부터 끝까지 완주 (튜토리얼 무엇이든)

**완료 기준**: 남의 코드를 붙여넣지 않고 목록·상세 화면을 만들 수 있다

---

### 1단계 — 동물병원 (가장 힘든 구간)

여기서 **앱의 뼈대 전체**를 만든다. 이후 단계는 이 위에 얹는 것이다.

**ETL**
- [x] Python 프로젝트 + GitHub 저장소 생성
      · **github.com/junkie300/pet (public)** — 2026-09-10 에 올렸다. 그전까지 `git remote` 가
        비어 있어서 **워크플로가 한 번도 돈 적이 없었다** (D-94)
      · ⚠️ **첫 실행이 `test.yml` 의 고장을 찾아냈다** — "표준 라이브러리만 쓴다"는 전제가
        틀려서 6개가 깨졌다(`pyproj`·`openpyxl`). 고쳤고 **지금은 초록**이다
- [x] 동물병원 원천 확보 — **행안부 15154952 활용신청** 완료. 10,617행을 받았다
      · 2단계 미용업(15154944)도 **같은 포털·같은 키**에서 신청한다 (D-93)
- [x] ⚠️ **좌표계 검증** — EPSG:5174 → 4326. 카카오 지오코딩 대비 오차 1m 이내로 검산했고
      (2097 은 321m 어긋난다 · D-50), **실기기 지도 위에서 눈으로도 확인했다** (D-73)
- [x] `상세영업상태코드`로 폐업·휴업 제외 — 영업중 5,474 (D-52)
- [x] 주소 → `region_code` 매핑 (0단계 테이블 사용) — 미배정 27건(0.5%)은 주소가 없거나
      개편 전 주소다. **시군구로 대충 채우지 않는다** (D-51)
- [x] 좌표 결측분 지오코딩 보완 — `lookup_kakao()`. 남은 좌표 없음 4건
- [x] `places` upsert (`on_conflict=source,source_id`)
- [◐] GitHub Actions cron 매일 실행 + `sync_logs` 기록
      · `sync_logs` 는 돌고 있고, 워크플로에서 **`hospitals` 를 수동 실행**할 수 있다
      · ⚠️ **cron 은 아직 꺼 두었다** — 켜면 매일 운영 DB 에 쓴다. 켜는 것은 사람이 정한다
        (`.github/workflows/etl.yml` 의 `schedule` 주석)
- [x] 실패 시 알림 — 워크플로가 실패하면 **이슈**로 올린다 (D-94)
      · 매번 새 이슈를 만들지 않는다. 같은 제목의 열린 이슈가 있으면 **댓글로 모은다** —
        매일 실패하면 이슈가 쌓이고, 쌓이면 안 읽는다
      · 함께 막은 것: **"성공했다는 거짓말".** upsert 는 지우지 않으므로 원본이 0건을 줘도
        조용히 success 가 찍히고, 앱은 낡은 데이터에 오늘 날짜를 붙인다 → `petetl/sanity.py`
        가 0건·급감(직전 성공의 절반 밑)에서 터뜨린다. 단위 테스트 16개
      · ✅ **실제로 도는 것까지 봤다** (2026-09-10) — 일회용 브랜치에서 일부러 실패시켜
        이슈 생성 · 두 번째 실패의 댓글 합류 · 본문 링크를 확인하고 흔적을 지웠다

**백엔드** — 0단계에서 스키마를 통째로 적용하면서 이미 끝났다
- [x] `places` 테이블 + PostGIS 인덱스 + RLS
- [x] `places_nearby` RPC

**앱**
- [x] 프로젝트 생성, Supabase SDK 연결 (`app/`, Kotlin 2.3.21 · AGP 8.13.2 — D-36)
- [x] S-01 지역 선택 — 3단 드롭다운 · 지역명 검색 · 최근 지역 3개 · **「현재 위치로」** (D-91)
      · 「현재 위치로」는 `nearestDong`(D-86)으로 **읍면동을 고른다** — 새 조회 경로를 만들지 않았다
      · ⚠️ **권한을 거절해도 지역 선택은 그대로 된다** (spec.md §5.2). 단위 테스트가 지킨다
      · **에뮬레이터에서 전 경로 확인 완료** — 3단 연동·최근 지역 유지·라이트/다크·오프라인 실패/회복
      ·      다만 위치를 **실제로 잡는 것**은 에뮬레이터가 못 한다 → 실기기에서 한 번 더 본다 (D-91)
      · ⚠️ D-39 로 **탭이 아니라 홈(S-00)의 지역 칩에서 여는 화면**이 됐다. 화면 자체는 그대로 산다
- [x] **S-00 홈 허브** — 지역 칩(읍면동까지) + 카테고리 6칸 그리드 + 건수 (D-39)
      · 건수는 PostgREST 직접 조회와 대조 완료(삼성동 8 · 대치동 7)
      · **적재 안 된 카테고리는 `0곳` 이 아니라 `준비 중`** — D-53
- [x] 하단 탭 4개 (홈 · 지도 탐색 · 구조·입양 · 더보기) — D-39
      · 지도·구조입양은 "무엇을 기다리는지" 적은 안내 화면. 더보기는 데이터 출처·앱 정보까지 실동작
      · ⚠️ 탭인 화면을 다른 탭 위에 push 하면 백스택이 되살아난다 — D-55
- [x] 동물병원 ETL — `run.py hospitals`. EPSG:5174 좌표 변환 · 주소로 읍면동 배정 ·
      폐업 제외 (D-50·D-51·D-52)
- [x] **장소 목록** — 읍면동 + 카테고리로 조회
      · 이름순이다. 거리순은 현재 위치가 있어야 하고 이 앱의 기준점은 고른 지역이다 (D-59)
      · 홈 타일은 지도로 간다(D-76). 목록 화면은 **지도를 못 여는 기기**의 경로다 (D-80)
- [x] **S-03 상세** (전화·길찾기·공유 + **출처·기준일 표기**)
      · ⚠️ 별점·후기·장소 사진은 **넣지 않는다** (D-40). 별점 자리에 `출처 · 기준일` 을 놓았다
      · ⚠️ **길찾기·전화는 지도 키 없이 됐다** — 대기 목록에서 뺀다 (D-57)
- [x] **네이티브 앱 키 발급 + 콘솔 플랫폼 등록** (2026-09-06) — 대기 끝났다
      · REST 키를 발급받은 **같은 앱**에 Android 정보를 등록했다: 패키지명 + debug 키 해시
      · ⚠️ 출시할 때 **release 키 해시를 하나 더** 등록해야 한다 (Play 앱 서명 인증서 기준)
- [x] **카카오맵 SDK 연동** (2026-09-06) — 카카오 저장소 추가 · 키를 `local.properties` → `BuildConfig` 로 (D-69·D-70)
      · ⚠️ SDK 가 **ARM 전용**이다. 초기화 실패로 앱이 죽지 않게 감쌌다 (D-71)
- [◐] S-02 지도 + 핀 + 클러스터링 + 바텀시트 3단
      · [x] ① 지도 + 핀 — **실기기(SM-S711N)에서 확인 완료** (D-70·D-73)
      · [x] ② 바텀시트 3단(peek/half/full) + 핀 탭 → 시트에서 강조 — 확인 완료 (D-74·D-75)
      ·      목록은 시트 안으로 들어갔다. **목록 화면과 같은 항목 코드**를 쓴다 (D-26)
      · [x] ③ 카테고리 필터 칩(복수 선택) + 홈 타일 → 지도 — 확인 완료 (D-76)
      ·      ⚠️ **칩 두 개를 켠 화면은 2단계(미용) 전까지 확인할 수 없다** — 적재된 카테고리가 하나다
      · [◐] ④ 클러스터링 (핀 50개 초과 · `spec.md §5.2`) — 코드·단위테스트 끝, **실기기 확인 남음** (D-78)
      ·      **SDK 에 클러스터러가 없다.** 화면 픽셀 격자로 직접 묶는다
      ·      그림·이름을 다른 레이어로 나눠, 이름이 겹쳐도 **핀은 사라지지 않는다**
      · [x] **「이 지역에서 다시 검색」** — `spec.md §5.2` 의 마지막 한 줄. **실기기 확인 완료** (D-86)
      ·      ⚠️ 경계 다각형이 없다 → **중심에서 3km** 로 뜨고, 누르면 **가장 가까운 읍면동으로 갈아탄다**
      ·      반경 조회(`places_nearby`)를 쓰지 않는다 — 새 조회 경로가 0개여야 한다
      · [x] 같은 좌표 장소의 **이름을 하나만** 그린다 — **실기기 확인 완료** (D-85)
      · [◐] 좌표가 **달라도** 겹치던 이름 — 경쟁에 순서가 없었다(`rank` 전부 0). 고쳤고 **실기기 확인 남음** (D-92)
      · [x] **`목록으로 보기`** — 지도를 못 여는 기기의 빠져나갈 길 (D-89 · D-80 의 나머지 반쪽)
      · [x] **다크 모드 지도 바탕** — `DimScreenLayer` 로 바탕만 덮는다. 확인 완료 (D-87)
      ·      카카오맵에는 밤 스타일이 없다. `DimScreenCover.Map` 이 아니면 핀까지 어두워진다
- [x] **S-07 즐겨찾기** (Room) — 상세의 북마크 · 더보기에서 목록
      · [x] 카드에 거리 — 목록·지도와 **같은 뜻·같은 코드**다 (D-88). 실기기 확인 완료
      · id 만이 아니라 **이름·주소를 함께 저장**한다. 안 그러면 오프라인에서 빈 목록이 된다 (D-60)
      · 앱 종료 + 網 차단 상태에서 그대로 뜨는 것을 확인했다
- [x] **캐시 우선(cache-first)** — 사본을 먼저 그리고 뒤에서 갱신 (D-79)
      · 저장소가 값을 두 번 흘린다(`Flow<Fetched<..>>`). 圈外 대기 8~16초가 사라졌다
      · **배너는 서버가 못 준 것이 밝혀진 뒤에만** 뜬다 (D-66)
      · **에뮬레이터 실측**: 網 차단 콜드 스타트 4.3초 = 온라인과 같다. 배너 깜빡임 없음
      · ⚠️ 목록(지도 시트)만 실기기 몫이다 — x86_64 에서는 지도가 안 열린다 (D-71)
- [x] 로딩·빈 상태·실패 상태 **구분** 처리 — `UiState` 로 타입에서 강제 (D-38)
- [x] **거리 표기** (`spec.md §6.4`) — 카드에 직선 거리. 위치는 `LocationManager` 로 얻는다 (D-81)
      · **먼저 묻지 않는다.** 목록의 "거리 보기"를 누른 다음에 권한을 묻는다
      · 목록 순서는 그대로 이름순이다 — 기준점은 현재 위치가 아니라 고른 지역이다 (D-24·D-59)
      · [x] 지도의 "현재 위치로" (D-82) · [x] 즐겨찾기 카드의 거리 (D-88) — 셋 다 실기기 확인 완료
      · **[실측]** 용인 기흥에서 역삼동 `25km` · 구갈동 `620m`. 에뮬레이터는 위치를 안 준다
- [x] 다크 모드 — Material 3 다이내믹 컬러 + 브랜드 팔레트 fallback
- [x] **앱 아이콘** — 시안을 브랜드 딥그린으로 옮긴 적응형 아이콘(전경·단색·크림 배경) — D-77
      · [x] 테마 아이콘(Android 13+) **실기기 확인 완료** — 단색 문턱을 고쳤다 (D-90)
      · `tools/make_launcher_icon.py` 로 다시 뽑는다. **내용은 중앙 66dp 원 안**에 넣는다
      · ⚠️ 단색 레이어에는 **반투명이 없다.** 문턱을 잘못 잡으면 얼룩으로 보인다 (D-90)
      · ⚠️ 남은 것: 스토어용 512×512 는 7단계에서
- [x] 실제 기기 테스트 — 에뮬레이터(android-33)·실기기(SM-S711N) 양쪽 완료 (2026-09-09)
      · ⚠️ **지도는 에뮬레이터로 검증할 수 없다.** ARM 기기가 있어야 한다 (D-71)
      · ⚠️ 거꾸로 **지도를 못 열 때의 안내 화면은 에뮬레이터에서만** 볼 수 있다 (D-89)
      · ⚠️ 남은 것: ④ 클러스터링을 **실데이터로** 보는 것 — 2단계 미용시설이 들어와야 문턱을 넘는다

**완료 기준**
- 임의의 읍면동을 골라 그 지역 동물병원이 지도와 목록에 정확히 뜬다
- 폐업 업소가 표시되지 않는다
- 네트워크를 끊어도 앱이 죽지 않고 오프라인 안내가 뜬다
- 모든 상세 화면에 기준일이 보인다

---

### 2단계 — 미용시설 (재사용 검증 ①) ✅ 완료 (2026-09-10)

- [x] 공공데이터포털 **`15154944` 활용신청** — 자동승인. 키는 이미 있는 `DATA_GO_KR_KEY` 그대로
      엔드포인트는 `https://apis.data.go.kr/1741000/pet_grooming/info` 로 **확정됐다** (D-93·D-95)
- [x] ETL: `hospitals.py` 를 재사용 — **변환 함수는 한 줄도 베끼지 않았다.** 소스마다 다른 것을
      `Dataset` 한 덩어리로 뽑고 `grooming.py` 는 그 값을 채운 설정 파일이다 (D-95)
      → **16,160행 적재 · 영업중 11,292 · 지역 미배정 31(0.2%) · 좌표 없음 508**
- [x] 앱: **`PlaceCategory.GROOMING` 의 `loaded` 한 줄** — 라벨·아이콘·색·홈 타일·지도 핀 색·
      필터 칩은 이미 전부 배선돼 있었다 (D-26·D-53)

**완료 기준 — 이게 진짜 시험이다**
> **앱 코드 수정이 "필터 칩 1줄 추가" 수준으로 끝나야 한다.**
> 화면을 새로 만들어야 한다면 1단계 설계가 잘못된 것이므로 **되돌아가 고친다.**

**→ 통과했다.** 앱에서 바꾼 것은 `loaded = false` → `true` 한 줄이다. 함께 손댄 것은 1단계
상태를 일부러 못박아 둔 **테스트 두 개**뿐이다(`PlaceCategoryTest`·`HomeViewModelTest`).

⚠️ **다만 원본이 병원과 결정적으로 달랐다 — 주소의 번지가 100% 가려져 온다** (D-95).
읍면동 배정은 앞 토큰이라 멀쩡하지만 **좌표 지오코딩은 못 한다**(읍면동 중심이 돌아온다).
그리고 `평화동*가` 처럼 **동 이름 속 숫자까지 가려지면** 이름으로는 영영 못 맞춘다 →
**좌표로 법정동을 역추적**해서 미배정을 **469 → 31건**으로 줄였다.

> 🎉 **클러스터링(D-83)이 드디어 실데이터로 문턱을 넘는다.** 영업중 기준 한 읍면동 최대가
> 30곳(송도동)이라 문턱 50에 못 닿았는데, 미용이 들어오며 **송도동 95곳**(병원 30 + 미용 65)이
> 됐다. **10개 읍면동이 50을 넘는다** — 이제 `THRESHOLD` 를 낮춘 빌드 없이 실기기에서 볼 수 있다.

---

### 3단계 — 야생동물구조센터 (재사용 검증 ②)

- [ ] ETL: 표준데이터 수집 → `category='wildlife_center'` (좌표 포함, 변환 불필요)
- [ ] 앱: 필터 칩 추가
- [ ] 상세에 "야생동물 발견 시 신고 안내" 문구

**완료 기준**: 2단계와 동일. ETL만 추가하고 앱은 거의 손대지 않는다.

---

### ★ MVP 비공개 테스트 시작 (3단계 완료 직후)

- [ ] Play 개발자 계정 등록 ($25)
- [ ] 개인정보처리방침 작성 및 URL 게시 (GitHub Pages 무료)
- [ ] 앱 콘텐츠 등급 설문 · 데이터 안전 섹션
- [ ] 서명 키 생성 및 **안전한 백업** (분실 시 앱 업데이트 영구 불가)
- [ ] 스토어 등록정보 (아이콘·스크린샷·설명)
- [ ] **비공개 테스트 트랙에 MVP 업로드**
- [ ] **테스터 12명 등록 → 14일 연속 opt-in 유지 시작**

> 이후 4~6단계는 **테스트가 돌아가는 동안** 진행한다.

---

### 4단계 — 동반 식당

- [ ] 식약처 Excel 다운로드 → 파싱 ETL (주 1회)
- [ ] 문화시설 데이터를 **보조**로 병합 (⚠️ 갱신 중단 데이터임을 기준일로 표기)
- [ ] 주소 → 좌표 지오코딩
- [ ] 앱: 필터 칩 + **안내 문구 고정 노출**
      *"목록에 없어도 동반 가능한 곳이 있을 수 있으니 방문 전 확인하세요"*

> ⚠️ Excel 구조가 바뀌면 ETL이 깨진다. 파싱 실패 시 **기존 데이터를 지우지 말 것.**

---

### 5단계 — 관광정보

- [ ] TourAPI 인증키 발급, 반려동물 동반여행 조회
- [ ] `category='tour'`, 이미지 URL을 `extra`에 저장
- [ ] 앱: 필터 칩 + 상세에 이미지·반려동물 정책 표시

---

### 6단계 — 보호센터 · 입양 ([입양] 탭 신규)

- [ ] ETL: `shelterInfo_v2` → `shelters` (주 1회)
- [ ] ETL: 구조동물 조회 → `animals` (매일). 공고 종료분 정리
- [ ] 앱: 하단 탭에 [입양] 추가
- [ ] S-04 입양 목록 (그리드, 사진 중심) + 필터(축종·지역·공고중)
- [ ] S-05 동물 상세, S-06 보호센터 상세 (지도 연동)
- [ ] **명칭을 "동물보호센터"로 표기** (민간 단체 미포함이므로)

---

### 7단계 — 정식 출시

- [ ] 프로덕션 액세스 신청 (테스터 12명 × 14일 충족 후)
- [ ] 전 화면 회귀 테스트, 크래시 점검
- [ ] S-08 데이터 출처 화면 최종 확인 (공공데이터 표기 의무)
- [ ] 스토어 심사 제출
- [ ] 심사 반려 대응 여유 1주 확보

---

## 3. 병행 트랙 — 테스터 12명 확보 ⚠️ 오늘 시작

**개발이 아니라 사람 문제이며, 마지막에 알면 최소 2주가 그냥 날아간다.**

요건: **12명 이상이 14일 연속 opt-in 상태 유지** (2023-11-13 이후 개인 계정)
※ 사업자 계정은 면제 — 사업자 등록이 있다면 이 트랙 전체가 불필요

- [ ] 지인·가족 중 **안드로이드 사용자** 목록 작성 (12명 + 예비 3명)
- [ ] 각자의 **Google 계정 이메일** 수집 (테스터 등록에 필수)
- [ ] 반려동물 커뮤니티·오픈채팅에서 추가 모집
- [ ] 참여 방법 안내문 준비 (opt-in 링크 클릭 + **14일간 삭제 금지**)
- [ ] 중도 이탈 모니터링

> 예비 인원을 반드시 확보할 것. 한 명이 중간에 앱을 지우면 카운트가 흔들린다.

---

## 4. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 1단계에서 학습 곡선에 막힘 | L단계를 먼저 완주. 2주 이상 정체 시 화면을 더 단순하게 축소 |
| 인허가 데이터 좌표 변환 실패 | 1단계 착수 즉시 검증. 실패 시 주소 지오코딩으로 우회 |
| 식약처 Excel 구조 변경 | 파싱 실패 시 기존 데이터 유지 + 알림. 자동 삭제 금지 |
| 공공 API 트래픽 한도 | 증분 수집 설계. 활용사례 등록으로 증량 신청 |
| 테스터 12명 미달 | 예비 3명 확보. 사업자 등록 검토 |
| Supabase 무료 한도 초과 | 사용량 모니터링. 초과 시 유료 전환($25/월) 검토 |
| 지도 쿼터 초과 | 목록 데이터는 자체 서버에서 가져오므로 지도 API 호출 최소 |

---

## 5. 비용

| 항목 | 비용 |
|---|---|
| Google Play 개발자 등록 | **$25 (1회)** |
| Supabase | 무료 티어 (초과 시 $25/월) |
| GitHub Actions | 무료 (public 저장소) |
| 카카오맵 API | 무료 (계정 첫 번째 앱) |
| 공공데이터 API | 무료 |
| 도메인 (개인정보처리방침) | 불필요 — GitHub Pages 무료 |
| **초기 총액** | **$25** |

---

## 6. 지금 당장 할 일 (Top 3)

1. **테스터 12명 명단 작성 시작** — 오늘. 개발과 무관하게 가장 리드타임이 길다.
2. **Supabase 프로젝트 생성 + `regions` 테이블** — 0단계 착수.
3. **공공데이터포털·카카오 개발자 계정 및 인증키 발급** — 승인에 시간이 걸릴 수 있다.
   (~~LOCALDATA~~ 는 2026-04-16 에 닫혔다 — 인허가 데이터도 공공데이터포털이다 · D-93)
